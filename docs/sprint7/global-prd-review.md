# 全局文档（product-design-specification.md + technical-architecture.md）Sprint 7 收口批量同步结论

> 产出：产品经理代理，2026-07-29（随 sp7 收口批量同步一并产出）
> 状态：**已应用（2026-07-29）**。`docs/sprint7/prd.md` 已于 2026-07-28 经 Maria 确认定稿 v1.0，点亮本项目既定的「Sprint PRD 定稿 v1.0 之后才批量同步全局文档」流程（旁证 = 走过该流程的 sprint 均留 `global-prd-review.md`，此前全仓仅 sprint5 / sprint6 两份）；Maria 于 2026-07-29 明确授权执行本次同步。R1~R17 已一次性批量应用：`technical-architecture.md` v1.2 → **v1.3**、`product-design-specification.md` v0.4 → **v0.5**。本文档留档作为两份文档的修订依据。

总体结论：本次是 sp7 的**第一次**全局同步（sp7 前四条需求 S7-01/02/03/05 此前只随交付零星回填、未走 review 文档流程），故一次性收口三档内容。与 sp5/sp6 两次「立项前审查」不同，本次是**交付后回填 + 承诺撤销 + 挂账登记**三合一：

- **A 档（已交付代码回填，R1~R14）**：S7-06（`d7cd8d3` 只读环境探测）与 S7-07（`8cf95c7` 探测由可选补充步改必做步骤）代码确在磁盘上，按全局架构文档「代码交付后回填」制回填。其中最值得记一笔的是——**§6.2「自动检测本地硬件配置」自 2026-05-06 写下起挂账 14 个月，本次终于由超前承诺转为已实现**。
- **B 档（承诺撤销，R15~R16）**：`§5.2 页面 3「用户环境配置表单」` 与 `§5.4 CLI --gpu "RTX 3090"` 两条已由 Maria 裁决撤销。
- **C 档（S7-08 不回填）**：S7-08（planning 平台感知规划）PRD §10 与架构 §18 均已定稿，但**代码零行**（`scale_reduced` / `local_fit_note` 全仓 grep 零命中，本次亲验），按回填制**不写进全局文档**，改在本文档 §二 立「待回填清单」。

共 17 项修改点（R1~R17：9 项【事实对齐·交付回填】+ 5 项【产品决策】+ 1 项【事实对齐·挂账登记】+ 2 项元数据）。另附三张清单：待 S7-08 回填清单（§二）、**「文档写了、代码没实现」承诺挂账清单（§三，逐条附文件:行号证据）**、留给 Maria 拍板的点（§四）。

---

# 一、已应用的修改（R1~R17）

## A 档：已交付代码回填（`technical-architecture.md`，v1.2 → v1.3）

### R1｜§3.1 节点定义表·`resource_scout` 职责行（:80）｜【事实对齐·交付回填】

- 原文（职责列）：`通过 deepxiv github_url、web search 搜索候选仓库（Sprint 6 MF-5 已移除 PwC 通道），git clone 后本地评估仓库质量；全自动完成仓库搜索与质量评分，不涉及用户交互`
- 改为（职责列句中插入 + 输出列追加）：`……本地评估仓库质量；**并在给出仓库结论前必做一步只读环境探测**（S7-06 交付、S7-07 由可选补充步升为必做步骤，见 §7.5），探得的本机事实经 local_env_facts 送达 planning；全自动完成，不涉及用户交互`；输出列由 `ResourceInfo（…）` 改为 `ResourceInfo（…）+ local_env_facts（本机实测环境事实，core/nodes/resource_scout.py:526-531）`
- 理由：该节点的职责与输出在 S7-06 后已实质变化（多了一路机器事实产出），节点定义表是全文的职责权威表，不改则下游各节引用的职责口径不一致。

### R2｜§3.2.1 各节点配置一览·`resource_scout` 行（:182）｜【事实对齐·交付回填】

- 原文：`| resource_scout | 20 | 4-7 | web_search, search_papers, get_paper_brief, git_clone_and_analyze, check_url_reachable（共 5 工具；Sprint 6 MF-5 摘除 PwC 通道后已无 search_pwc） |`
- 改为：`| resource_scout | 30（REACT_MAX_ROUNDS_RESOURCE_SCOUT，S7-07 由 20 上调） | 4-8 | web_search, search_papers, get_paper_brief, git_clone_and_analyze, check_url_reachable, **probe_environment**（共 6 工具；…；S7-06 新增只读环境探测，装配处 core/nodes/resource_scout.py:711-717） |`
- 核实证据：工具装配 `core/nodes/resource_scout.py:711-717`（6 个 lambda 项，末项 `make_probe_environment_tool`）；轮次 `config.py:70` `REACT_MAX_ROUNDS_RESOURCE_SCOUT: int = 30`。预期消耗 4-7 → 4-8 依据 S7-07 真跑实测（轮数由 3 增至 4）。
- 理由：2026-07-28 那一批刚把该行由 6 工具订正为 5（摘 PwC），本次是**同一格的第二次变更**（+1 只读探测工具），若不同步会立刻与 §3.1 / §5 / §7.5 三处自相矛盾。

### R3｜§3.2.1 配置表下·新增 S7-06 / S7-07 注（:190-191）｜【事实对齐·交付回填】

- 新增（接在既有 S7-03 注之后，同一 blockquote 内）：一段说明工具集 5→6 与轮次 20→30 的成因——`base_dir` 由工厂闭包绑定 `state["workspace_dir"]`（缺省回退 `config.WORKSPACE_DIR`），是该节点 `get_tools` lambda **首次使用 `state` 形参** ⇒ `bind_tools` 前缀"破一次"（一次性静态变更，跨论文仍字节一致）；S7-07 把探测段落由"可选补充步"改"必做步骤"并**删除段内一切轮次紧张暗示**（`core/nodes/resource_scout.py:104-109`），轮次上限同步 20→30（`config.py:70`）**作保险而非主因**——agent 对轮次是否紧张的全部认知只来自 prompt 措辞。
- 理由：与既有 S7-01 / S7-03 注的体例对称。且"删措辞是主因、放大数字是保险"这条因果若不写进架构文档，日后有人回调轮次上限会误以为动的是主因。

### R4｜§5 模块结构树·`core/tools/` 段（:549）｜【事实对齐·交付回填】

- 新增一行：`│       ├── env_probe_tool.py     # resource_scout 只读环境探测工具（probe_environment，整条命令允许清单 15 条 + 30s 超时 + 2500 字节返回上限，S7-06；见 §7.5）`
- 核实证据：文件存在（`core/tools/env_probe_tool.py`，13258 字节）；清单 15 条（`:75-82`，`len(_PROBE_COMMANDS) == 15` 实跑核实）；`_PROBE_TIMEOUT_SECONDS=30`（`:95`）；`_PROBE_OUTPUT_MAX_BYTES=2500`（`:107`）。
- 理由：模块结构树是"磁盘实况"表（2026-07-05 v1.1 曾专门按磁盘重写过一次），新增文件不入树即产生结构性失真。

### R5｜§7.5 只读环境探测边界·标题与状态行（:729 / :731）｜【事实对齐·交付回填】

- 原文（标题）：`### 7.5 只读环境探测边界（S7-06）`；（状态行）：`> **状态：设计已裁决 / 代码未交付**（2026-07-28）。……**其描述的 core/tools/env_probe_tool.py 当前尚不存在**。代码交付后删除本状态行并回填源码行号锚点。`
- 改为（标题）：`### 7.5 只读环境探测边界（S7-06 / S7-07 已交付）`；（状态行）：`> **状态：代码已交付**（2026-07-29 回填；S7-06 d7cd8d3、S7-07 8cf95c7）。落点 core/tools/env_probe_tool.py（新增）+ core/nodes/resource_scout.py 装配与渲染 + core/state.py:296 local_env_facts。裁决见 docs/sprint7/architecture.md §14~§17，需求见 docs/sprint7/prd.md §2.6 / §9。`
- 理由：**这是本次最必须改的一处**——旧状态行白纸黑字写着"该文件当前尚不存在"，而文件已在磁盘上，属直接的事实错误。该节当初是「代码交付后回填」制下的**一次有意识破例**（Maria 拍板先落盘，条件是三重状态标记 + 回填待办，见 `docs/TODO.md` 2026-07-28 条），本次即为兑现该待办（`docs/TODO.md:519`）；不摘除则破例永久化，正是本项目文档欠账反复复发的根因形态。

### R6｜§7.5 机制底线段·回填源码行号锚点（:735 / :749 / :757 / :759）｜【事实对齐·交付回填】

破例期间该节**全段刻意不写任何源码行号锚点**（行号锚点是"代码已交付"的隐含信号）。代码交付后按同一纪律**反向回填**，四处：

- （a）判定形态段（:735）：补 `core/tools/env_probe_tool.py:85-86`（模块级预解析 `_ALLOWED_ARGV`）、`:204` 单一判定、`:204-210` 未命中即结构化拒绝并 return 不触达 `_run_subprocess`，并补"允许清单为 15 条整命令（`:75-82`，覆盖 GPU/驱动/CUDA、CPU/内存/架构、磁盘、Python 环境、工具链五类）"。
- （b）双用途边界对照表·落点行（:749）：`core/tools/env_probe_tool.py（新增）` → `core/tools/env_probe_tool.py:177-251（S7-06 新增）`。
- （c）探测结论落点段（:757）：补 `core/state.py:296` 定义 / `:389` 初始化、`core/nodes/resource_scout.py:526-531` 非空才写、`core/nodes/planning.py:308` 第 6 形参 / `:357-358` 非空才入 payload / `:723` 实参传入。
- （d）执行参数段（:759）：把"约为短超时的四分之一 / 输出上限的更小量级"这类**模糊表述换成实值 + 行号**——`_PROBE_TIMEOUT_SECONDS=30`（`:95`，对比 `config.RUN_COMMAND_TIMEOUT=120`）、`_PROBE_OUTPUT_MAX_BYTES=2500`（`:107`，对比 `config.SANDBOX_OUTPUT_MAX_BYTES=1MiB`）、统一截断阈值 `config.TOOL_RESULT_MAX_LENGTH=8000`；并补上 2500 这个值的**实测来由**（600 行 `pip list` 包成 JSON 达 16111 字符 → 被 8000 截断成残缺 JSON → 解析返回 `None`，全程无异常无日志）。
- 理由：破例的三个必要条件之一就是"未交付不写行号"，交付后必须反向兑现，否则该节永远停在"看起来像设计稿"的形态，读者无法据它定位代码。

### R7｜§7.5 末·新增「已知缺口（当前实现，未修）」blockquote（:761）｜【事实对齐·挂账登记】

- 新增内容：返回端 2500 之外，摘要渲染端还有一道**方向相反**的截断——`sandbox/local_venv.py` 输出截断保留**尾部**，而 `core/nodes/resource_scout.py:57` 的 `_PROBE_OUTPUT_MAX_CHARS=400` 在 `:496` 处按**头部**截断，且 400 < 返回端 2500 ⇒ 返回端刻意保尾留下的内容被摘要端重新按头切掉，依赖多的宿主机上 `pip list --format=freeze` 里字母序靠后的 `torch` / `transformers` 仍可能在摘要端丢失。已登记为 R-S7-25 的渲染端复发（`docs/sprint7/architecture.md` §16.7 / §18.3），**修法与取值已裁决但代码未交付**，待交付后回填本节。
- 核实证据：`core/nodes/resource_scout.py:57` `_PROBE_OUTPUT_MAX_CHARS: int = 400`、`:496` `out = out[:_PROBE_OUTPUT_MAX_CHARS]`。
- 理由：这是本次回填时暴露的一条"交付了但只交付了一半"——返回端已根治、渲染端原样复发。**只写"已实现"不写这条，等于用一次回填制造一条新的隐性欠账**。写法上刻意只描述**现状缺口**、不写 S7-08 的新值与新常量（守 C 档边界）。

### R8｜§12.5 第二层节点层降级策略·`resource_scout` 搜索策略（:1114）｜【事实对齐·交付回填】

- 原文末句：`agent 通过 check_url_reachable 和 git_clone_and_analyze 工具自主验证和评估每个候选仓库。`
- 改为（句末追加）：`**只读环境探测（S7-06 / S7-07）不参与该降级链**：它是给出仓库结论前的一步必做动作（core/nodes/resource_scout.py:104-109），探测结果不改变三级搜索链的顺序与判定，探不到也不构成"找不到仓库"、不触发 from_scratch。`
- 理由：该节原文只描述"deepxiv → web search → from_scratch"三级链，新增的必做探测步若不划清与降级链的关系，读者会误以为探测失败也走 `from_scratch`——而代码的语义恰恰相反（prompt 明写"任何探测结果都不构成找不到仓库"）。

### R9｜§12.9 各节点错误分类全景·`resource_scout` 行（:1234）｜【事实对齐·交付回填】

- 原文：`| resource_scout | git clone 失败、websearch 失败 | 零资源 | deepxiv github_url → web search → from_scratch（Sprint 6 MF-5：PwC 通道已移除） |`
- 改为：瞬态列补 `**环境探测超时 / 本机无该命令**（S7-06）`；降级策略列补 `**环境探测类失败不进降级链**：命令被拒、超时、本机无该命令、输出为空一律走结构化返回（不抛异常、不进 degraded_nodes），local_env_facts 保持空串即表示"未知"，不造哨兵值、不阻断（core/tools/env_probe_tool.py:204-251 / core/nodes/resource_scout.py:526-531）`
- 理由：错误分类全景表是"这类错误怎么处理"的唯一权威表；探测引入了一整类新的失败形态（被拒 / 超时 / 命令不存在 / 空输出），不入表则该类错误的处置口径在全局文档里缺位。

### R10｜文档头元数据（:4 / :6）与文末更新日志（:1367）｜【事实对齐】

- 原文：`**版本**：v1.2`、`**最后更新**：2026-07-28`
- 改为：`**版本**：v1.3`、`**最后更新**：2026-07-29`；文末更新日志追加一条 2026-07-29 条目，逐条描述 R1~R9 并显式声明「**本次明确不含 S7-08**（`scale_reduced` / `local_fit_note` 全仓 grep 零命中、代码零行，按回填制不预写；待回填清单见本文档）」。
- 理由：与既有更新日志范式一致；v1.3 这个版本号本就是 2026-07-28 那批留下的约定（当时明确写"版本号本次不升，留待 sp7 收口升 v1.3"），本次兑现。该日志条描述 R1~R9 全部内容，必须与之同批应用。

## A 档：已交付代码回填（`product-design-specification.md`，v0.4 → v0.5）

### R11｜§3.2 步骤 3·建议 Agent 角色行（:136）｜【事实对齐·交付回填】

- 原文：`Resource Scout Agent -- 通过 deepxiv github_url、web search 搜索关联仓库，通过 git clone 拉取代码并评估仓库质量（官方性、完整性、最近提交时间），自动推荐最优选择`
- 改为（句末追加，全通俗中文、零内部术语）：`……自动推荐最优选择；**并在给出仓库结论前必做一步本机环境探测**（Sprint 7 已上线）：在这台机器上跑几条只读的查询命令，问清有没有显卡、驱动与 CUDA 版本、磁盘还剩多少、装了哪些常用包，探到的结果作为"本机实测事实"随复现计划一并传给下一步。这些命令只能查、不能装不能删不能下载，探不到（比如这台机器没有显卡、没有该命令）同样是一条有效结论，不影响仓库搜索的结果，也不会让流程中断`
- 理由：`docs/TODO.md:518` / `:534` 两处早已登记"sp7 收口一次性补 §3.2 步骤 3 Agent 能力枚举"，本次兑现。措辞严守「用户可见文案零术语」通则（sp5 R7 立的通则）：不出现 `probe_environment` / `local_env_facts` / `resource_scout` 任何一个英文标识。

### R12｜§6.2 本机模式·第 1 句（:772）｜【事实对齐·交付回填，本次同步的价值锚点】

- 原文：`- 自动检测本地硬件配置（GPU 型号、显存、CPU、内存）`
- 改为：`- **自动检测本地硬件与软件环境（Sprint 7 已上线）**：在资源搜集阶段自动跑几条只读的查询命令，问清这台机器有没有显卡、驱动与 CUDA 版本、CPU 与内存、磁盘可用空间、Python 与常用工具链版本、已装的包；探测结果作为"本机实测事实"传给复现规划。命令只查不改（不装、不删、不下载），探不到或本机没有该命令同样是一条有效结论，不阻断流程。**当前实测覆盖以显卡 / CUDA / 磁盘三项为主**，其余维度的必探要求随后续迭代补齐`
- 理由：**这一条是本次同步的意义所在**——该承诺自 2026-05-06 初稿写下起就是超前承诺（代码从来没实现过），2026-07-28 那批曾明确"故意不清，S7-06 交付后自动变准"。现在它变准了。末句的"当前实测覆盖以显卡 / CUDA / 磁盘三项为主"是**如实标注、不夸大**：真跑实证探的正是 `nvidia-smi` / `nvcc --version` / `df -h .` 三条，CPU / 内存 / Python / 已装包虽在允许清单内（`core/tools/env_probe_tool.py:75-82`）但 prompt 未点名、从未被探（`core/nodes/resource_scout.py:105` 只点名三项）。**不写这句就是把"部分实现"包装成"全部实现"，与 sp5 立下的诚实性纪律直接冲突。**

### R13｜§6.2 本机模式·第 2 句（:773）｜【事实对齐·挂账登记】

- 原文：`- 根据硬件约束自动调整复现参数`
- 改为：`- 根据硬件约束自动调整复现参数 —— **本条尚未实现，已转 backlog（2026-07-28 Maria 裁定）**：Sprint 7 的承诺边界止于"把本机实测事实真正送到复现规划可见的地方"，按显存自动算 batch size、梯度累积、分布式配置这类逐参数精调不在本轮范围，需要时另行立项`
- 理由：Maria 2026-07-28 已裁该条转 backlog（非永不做）。**必须白纸黑字标在正文而非只写在 TODO**——否则第 1 句变准之后，第 2 句会被顺势读成"也实现了"，正是「承诺蒸发」的典型发生方式。注：S7-08 已**部分推翻**这条（纳入"本机不够时按本机可跑规模改写执行步骤"），但 S7-08 代码零行 ⇒ 本次维持"尚未实现"表述，待 S7-08 交付后再改（见 §二）。

### R14｜§7.1 MVP 表（:824）+ §8.1 v1.x 核心完善（:865）｜【产品决策】

- §7.1 在 Sprint 7 既有行之后新增：`| 本机环境自动探测（Sprint 7） | P1 | 资源搜集阶段自动查清这台机器的真实情况（显卡、驱动与 CUDA 版本、CPU 与内存、磁盘余量、Python 与常用包），只查不改、探不到不阻断，结果作为"本机实测事实"传给复现规划（详见 §6.2 / docs/sprint7/prd.md §2.6 与 §9） |`
- §8.1 在 Sprint 7 条目之后新增一条立项条目，含从"工具做好了但 agent 从不调用（真跑触发 0 次）"到"改必做步骤后 0 → 3"的实证过程，并显式点明「**这条补的正是 §6.2 自 2026-05-06 起就写下、却一直没有实现的承诺**」。
- 理由：与 sp4/sp5/sp6 入表先例对称，功能清单与路线图须反映 sp7 的这条交付。把"触发 0 次"这段实证写进路线图条目，是因为它是本 sprint 最有说服力的一条经验（工具可用 ≠ 被使用），也是 S7-08 立项的直接前因。

## B 档：Maria 已裁决撤销的承诺（`product-design-specification.md`）

> **两条同源，撤销理由共用**：①**本机实测比人手填更准**——S7-06/S7-07 已打通探测通道并真跑验证（`core/tools/env_probe_tool.py` + `core/nodes/resource_scout.py:104-109`），系统能自己查清显卡 / CUDA / 磁盘，用户手填只会更不准且更麻烦；②**远程执行本次不做**（Maria 2026-07-29 裁定）——`core/state.py:365` `sandbox_type` 写死 `"venv"`、全仓无远程底座，与 §7.2「云服务器远程执行 → v2」一致 ⇒ **不存在"填另一台机器的规格"这个用例**；③保留它会与"本机优先"的产品主张**互相打架**：一边说以本机实测为准，一边留个表单让用户手填另一套规格，两者冲突时听谁的没有答案。④**不撤销就得实现**，否则又是一条挂账。
> 旁证（本次亲自上磁盘核实）：全仓无 `auto-repro` CLI；`scripts/` 下只有 `run_paper.py` 等开发脚本，其 `argparse` 仅 `--arxiv-id` / `--no-save` 两个参数（`scripts/run_paper.py:119-121`）。

### R15｜§5.2 页面 3 复现计划审核页·核心元素「用户环境配置表单」（:683）+ 交互流程（:688）｜【产品决策】

- 原文（核心元素）：`  - 用户环境配置表单（选择/输入 GPU 型号、内存等）`
- 改为：`  - ~~用户环境配置表单（选择/输入 GPU 型号、内存等）~~ **已撤销（Sprint 7 收口，2026-07-29）**：系统已能自动探测本机真实环境（见 §6.2），实测比人手填更准；而"填另一台机器的规格"这个用例依赖远程执行，远程执行不在 MVP 范围（见 §7.2）⇒ 该表单没有使用场景，保留它会与"以本机实际情况为准"的产品主张互相打架`
- 原文（交互流程）：`用户阅读计划 -> 查看候选仓库选择（…）-> 查看信息完整度评估（如有降级）-> 配置环境信息 -> 确认或要求修改 -> 进入执行或修改循环`
- 改为：`…-> 查看环境需求与本机实测情况的匹配提示（无需手工填写环境信息，见上）-> 确认或要求修改 -> 进入执行或修改循环`
- 采用**删除线保留 + 写明撤销理由**而非整行抹掉：与 §7.1「~~Papers With Code 搜索~~ 已摘除」（sp6 R7 先例）体例一致，让日后读者能看到"撤销过什么、为什么"，避免同一诉求被重新提出。
- ⚠️ 交互流程那一步**必须同改**：只删核心元素不改流程，会留下一步指向已撤销组件的操作，是同族欠账的典型残留形态。

### R16｜§5.4 CLI 入口设计（:737 状态说明 / 删 `--gpu` 示例行 / :755 撤销说明）｜【产品决策 + 事实对齐】

- 删除原示例行：`auto-repro run --paper 2409.05591 --env local --gpu "RTX 3090"`（连同其上方注释 `# 指定配置`）
- 节首新增状态说明（:737）：`> **实现状态说明（2026-07-29 核实后补记）**：本节整节为**设计构想，尚未实现**——仓库内不存在 auto-repro 命令，现有唯一的命令行入口是开发用的 scripts/run_paper.py（只接受 --arxiv-id 与 --no-save 两个参数）。本节以下命令示例请按"规划中的形态"阅读，勿当作可用功能。CLI 在 §7.1 中的定位为 P1。`
- 节末新增撤销说明（:755）：写明 `--env local --gpu` 那行的删除理由（与 R15 同源），并显式声明「**其余命令仍为未实现的设计构想，本次一并登记、未删除**：是否实现、实现到哪一步待产品决策」。
- 理由：撤销那一行是 Maria 已裁决的动作；而**整节未实现这件事属新发现的既有欠账**，按任务边界"一并登记但不擅自删"处置——`auto-repro run / plan / code-only / status` 四条命令全部不存在，若只删一行不加状态说明，读者仍会以为其余三条可用。§7.1 MVP 表里「CLI 基础入口 P1」这行**本次不动**（见 §四·待拍板 3）。

## 元数据

### R17｜`product-design-specification.md` 文档头（:4 / :6）与文末更新日志（:931）｜【事实对齐】

- 原文：`**版本**：v0.4`、`**最后更新**：2026-07-28`
- 改为：`**版本**：v0.5`、`**最后更新**：2026-07-29`；文末更新日志追加一条 2026-07-29 条目，分「一、已交付代码回填 / 二、承诺撤销两条 / 三、事实对齐」三段描述 R11~R16，并显式声明本次不含 S7-08 及其理由。
- 理由：与既有范式一致（v0.2→v0.3 随 sp5、v0.3→v0.4 随 sp6，均绑定「Sprint PRD 定稿 v1.0 + Maria 确认」这一事件）；v0.5 这个号本就是 2026-07-28 那批留下的约定（"留待 sp7 收口时统一升 v0.5"），本次兑现。该日志条描述 R11~R16 全部内容，必须与之同批应用，不可先行单独落。

---

# 二、待 S7-08 代码交付后回填清单（本次**不写**进全局文档）

**不写的依据**：全局架构文档走「代码交付后回填」制（架构师已用 git 证据论证过：sprint5/sprint6 两次 PRD 定稿期间 `technical-architecture.md` 零更新，`8d37fe9` 则是 code+test+doc 同批）；全局产品文档 §7.5 那次先落盘属 Maria 拍板的**一次有意识破例**、且以三重状态标记为必要条件，不构成惯例。

**S7-08 代码零行已核实**（2026-07-29 本次亲验）：`grep -rn "scale_reduced\|local_fit_note" core/ ui/ tests/ sandbox/ config.py app.py` **零命中**。

| # | 待回填内容 | 落点（全局文档） | 依据 |
|---|---|---|---|
| 1 | **缩规模声明**：实验被缩减规模时全链路贯穿并在报告强制声明，且**不得评为科学复现** | 产品 §1.3「结果可信」行 / §4.5.2 核心标准 / §4.6.1 报告第 4 条；架构 §12.9 或报告章节 | PRD §10.2 目标 5、§10.9 已拍板第 6 条；**注**：§1.3/§4.5.2/§4.6.1 三处**现已承诺"缩减规模强制声明"但 core/ 零实现**，见 §三 挂账清单第 5 条 |
| 2 | **本机不够时的两选项询问**：(a) 缩小规模仍在本机真跑 / (b) 只产代码不执行，映射到审核面板**已有的两个按钮**，不新增中断种类与决策类型 | 产品 §5.2 页面 3 交互流程 / §4.3.3 平台感知的规划 | PRD §10.4 场景 B、§10.6、A-S7-14 |
| 3 | **审核页新增只读展示块**：本机实测事实原文、够不够 + 缺口、本次预计占用（GPU 张数 / 显存 / 磁盘增量 / 预计时长）、若缩了规模则写清缩了什么 | 产品 §5.2 页面 3 核心元素 | PRD §10.6；**这正是本次撤销的"用户环境配置表单"腾出来的位置**——由"让用户填"改为"给用户看" |
| 4 | **审核页讨论助手能看到本机事实**（`_format_plan_context` 加第 4 键） | 产品 §5.2 页面 3（若需写到产品层）；架构相应节 | PRD §10.9 已拍板第 9 条 |
| 5 | **三级优先级契约**：本机实测 > 论文推断 > 明确写"未探测/未知"；探测未覆盖的维度**不得给具体数值** | 产品 §4.3.1 计划内容「2. 环境需求」；架构 planning 相关节 | PRD §10.2 目标 2/3、AC-S7-32/33 |
| 6 | **六项必探维度**：GPU/显存与占用、CUDA、内存、磁盘、Python 版本、关键包版本 | 产品 §6.2 第 1 句末的"当前覆盖三项"表述（R12 留的接口）；架构 §7.5 | PRD §10.3 #1、AC-S7-41 |
| 7 | **§6.2 第 2 句的部分转正**：从"尚未实现、已转 backlog"改为"本机不够时按本机可跑规模改写执行步骤已实现；逐参数精调仍 backlog" | 产品 §6.2 第 2 句（R13 留的接口） | PRD §10.3 #2、§10.8 第 1 条（对 S7-06 非目标 7 的**部分**合法推翻） |
| 8 | **摘要渲染端上限修复**：`_PROBE_OUTPUT_MAX_CHARS` 调值 + 新增摘要总长上限 | 架构 §7.5「已知缺口」blockquote（R7 留的接口，届时摘除） | 架构 §18.3；本次只登记现状缺口、未写新值 |
| 9 | **§7.1 / §8.1 增 S7-08 行**（沿 sp4/5/6 入表体例） | 产品 §7.1 MVP 表 / §8.1 v1.x | 与 R14 同体例 |
| 10 | **§5.2 页面 3「环境需求提示（与用户当前环境的匹配情况）」由未实现转已实现** | 产品 §5.2 页面 3 | PRD §10.3 #10：`ui/pages/plan_review.py` 全文 grep `GPU`/`硬件`/`用户环境`/`环境配置` 零命中（见 §三 挂账清单第 4 条） |

> **该清单的存在理由**：定稿点亮的下游流程若无人登记，就是下一个"条件写了没人回头看"。本项目文档欠账反复出现的根因正在于此。

---

# 三、「文档写了、代码没实现」承诺挂账清单（逐条附 **文件:行号** 证据）

> **纪律**：判定"已实现"一律附源码行号，**不拿文档证明文档**。本清单是 sp7 PRD §10.3「23 条逐条三选一核对表」的延伸——把那张表里判为"未实现"的、以及本次新核出的，集中成一张可追踪的账。
> **本次一条都没有擅自改**（除 R13 / R16 两处已获明确授权的标注外），全部只登记、待 Maria 拍板。

| # | 承诺原文（出处） | 核实结论 | 核实证据（本次亲验） | 建议处置 |
|---|---|---|---|---|
| 1 | `technical-architecture.md` §12.10 依赖安装阶段：「单包失败时: **尝试降级版本 → 无版本约束安装** → 标记失败」 | **未实现** | `sandbox/local_venv.py:657-672` 逐包安装循环：单包 `exit_code != 0` 后**直接** `install_failed_packages.append(pkg)`（`:672`）+ `overall_success = False`，**无任何版本降级、无去版本约束重试**。唯一的重试是 `_pip_install_with_retry`（`:470` 起），它只对**网络瞬态**错误按指数退避重试**同一条命令**，不改版本约束。⚠️ **同块的「逐包安装（非批量）」是真实现**（`:657` `for pkg in requirements or []`），**勿一起改** | ⟦2026-07-29 已处置·见 §四 第 1 条⟧ **已改文档为如实描述真实形态**（而非标注「规划未实现」）——主控追查失败包下游发现版本调整已交由 execution agent 承担（`execution.py:1004` 工作纪律第 1 条明写「可用 run_in_sandbox 执行 pip install 兜底**或调整版本**」）。原 PM 倾向**改文档标注**——版本降级需要知道"降到哪个版本"，而这信息只有 pip 解析错误里才有，做对成本远超收益；且现有"逐包安装 + 失败包列表反馈给 coding 找替代方案"这条路已经能兜住 |
| 2 | `technical-architecture.md` §12「预防层: **调用 LLM 前估算 token 数，超限时主动截断/分段**」 | **未实现** | `core/llm_client.py:442-449` 定义 `estimate_tokens`、`:452-455` 定义 `check_context_limit`——但 **`check_context_limit` 在生产代码里零调用点**：全仓 grep 只命中自身定义、`tests/test_sprint1_smoke.py:67,75`（仅 import + `callable` 断言）与若干 sprint1 历史文档，`core/` `ui/` `sandbox/` `app.py` **零命中**。即"能估"但"没接"。真实机制只有**事后分类** `LLMContextOverflowError`（`core/errors.py:84`、`core/llm_client.py:174`）+ 工具结果定长截断（`config.TOOL_RESULT_MAX_LENGTH=8000`）。**预算口径确认为按调用次数计**：`config.py:31` `MAX_TOTAL_LLM_CALLS=240`、`core/state.py:228/371` `retry_budget_remaining`、`core/nodes/execution.py:1927` `total_calls = react_rounds_used + llm_calls_used`——**全程与 token 数无关** | ⟦2026-07-29 **Maria 裁：下个 sprint**，与 token/成本追踪 + 可观测性合并立项⟧ 原登记=**本次未改、待拍板**：补实现 vs 改文档标注「规划未实现」。PM 倾向**改文档标注**并**顺带提一句**：真要做"调用前估算"，其最大价值不在截断（事后 `LLMContextOverflowError` 已能兜），而在**成本追踪**——而成本追踪已由 Maria 定为下个 sprint 的可观测性 B 档（`docs/TODO.md:584`），两者应合并立项，不宜在本条单独补一个没有消费方的估算 |
| 3 | `product-design-specification.md` §4.2.2 仓库质量评估维度「**兼容性**：与用户环境的兼容程度」 | **未实现** | `core/nodes/_repo_scoring.py:14-21` `REPO_QUALITY_SCORING_SECTION` 五维权重 = `is_official 0.35` / `last_commit_date 0.20` / `commit_count_recent 0.15` / `has_readme+has_requirements 0.15` / `dir_structure 0.15`，**无兼容性维度**（本次逐字核实属实） | **措辞本次不动（已由 Maria 转 backlog）**，状态登记为「**已登记 backlog**」。不做的技术理由：该常量由 planning 与 resource_scout **共享同一对象**（`tests/test_sprint2_s2_13.py:148-149` 断言 `is` 同一），改它 = 同时改两个节点的冻结前缀，属零改动红线。对应 R-S7-40 |
| 4 | `product-design-specification.md` §5.2 页面 3「环境需求提示（**与用户当前环境的匹配情况**）」 | **未实现** | `ui/pages/plan_review.py` 全文 grep `GPU` / `硬件` / `用户环境` / `环境配置` **零命中**；`environment` 仅以**默认折叠的原始 JSON** 呈现（`:290-293`） | **本次不动**，S7-08 交付后转已实现（见 §二 第 10 条）。**注意与 R15 的关系**：本次撤销的是同页的"配置表单"（让用户填），保留的是这条"匹配情况提示"（给用户看）——两者方向相反，撤前者恰恰是为了让后者成为唯一入口 |
| 5 | `product-design-specification.md` §1.3 结果可信「实验为模拟或**缩减规模**时强制声明」+ §4.5.2 + §4.6.1 第 4 条（三处） | **未实现**（"模拟"那半已实现，"缩减规模"这半从未实现） | `core/` 全仓无任何"缩规模"产品机制；`core/nodes/reporting.py:229-288` 三条正交标注为 `simulation` / `credential_degraded` / `incomplete_execution`，**无规模维度** | **本次不动**，S7-08 交付后转已实现（见 §二 第 1 条）。这是与 §6.2 同款的承诺蒸发形态，且正好压在 S7-08 选项 (a) 上 |
| 6 | `product-design-specification.md` §4.4.1「参数适配：根据用户硬件调整训练参数」 | **未实现** | `core/nodes/coding.py:410-413` 只消费**论文侧** `hardware_requirements_en`；`local_env_facts` 全仓仅存在于 3 个文件（`core/nodes/resource_scout.py` 产出 / `core/state.py` 定义 / `core/nodes/planning.py` 消费）⇒ **coding / execution / reporting 完全看不到本机事实** | **本次不动**。S7-08 靠"计划文本 + 缩规模指令"间接解决（其非目标 8 明确不给 coding/execution 直连本机事实），不构成本条的完整兑现 ⇒ 本条在 S7-08 交付后**仍应保留在本挂账清单里** |
| 7 | `product-design-specification.md` §5.4 CLI 全节四条命令（`auto-repro run / plan / code-only / status`） | **全部未实现** | 全仓无 `auto-repro` 命令；`scripts/` 下仅开发脚本，`scripts/run_paper.py:119-121` 的 `argparse` 只有 `--arxiv-id` / `--no-save`（⟦2026-07-29 订正⟧ 它**会驱动完整主图**，但无中断续跑、输出只报告前两节点，详见 §四·补充） | **本次已加节首状态说明（R16），命令本体未删**，待 Maria 拍板：实现 / 缩范围 / 整节撤销。另见 §四·待拍板 3（§7.1「CLI 基础入口 P1」行的口径） |
| 8 | `product-design-specification.md` §4.3.3 第 1 条「本机单 GPU 时：调整 batch size、建议梯度累积」/ 第 2 条「多 GPU 服务器时：配置分布式训练脚本」 | **均未实现** | 与第 6 条同源（`core/nodes/coding.py:410-413` 只看论文侧字段）；分布式配置全仓无对应机制 | **本次不动**。第 1 条待 S7-08 部分兑现（本机 GPU 事实进计划、影响可行性判定），逐参数精调仍 backlog；第 2 条 Maria 已明确不做（远程不做 + §9.2 Q6 维持 v2） |
| 9 | `technical-architecture.md` §7.5「摘要渲染端 400 字符上限使 `torch` / `transformers` 可能丢失」 | **本次新登记**（S7-06 交付的一半） | `core/nodes/resource_scout.py:57` `_PROBE_OUTPUT_MAX_CHARS=400`、`:496` 按头截断；返回端 `core/tools/env_probe_tool.py:107` `_PROBE_OUTPUT_MAX_BYTES=2500` 保尾 ⇒ 外层作废内层 | **已写进架构 §7.5「已知缺口」blockquote（R7）**，待 S7-08 交付后摘除（见 §二 第 8 条） |

> **这张表的方法论要点**：全局文档反复出现"写了代码没实现"的超前承诺（§6.2 那条挂了 14 个月），根因不是有人偷懒，而是**没有一个地方逼着人拿行号说话**。本表与 sp7 PRD §10.3 的核对表构成同一道纪律：**判"已实现"必须附文件:行号，判"未实现"必须给处置建议与责任人**。

---

# 四、留给 Maria 拍板的点

> ⟦2026-07-29 当日更新⟧ 五条已全部经 Maria 逐条表态，**4 条已处置、1 条待她回话**。下表「裁决」列为最终结论，PM 原建议保留以备回溯。

| # | 事项 | 现状 | PM 原建议 | **Maria 裁决 / 处置** |
|---|---|---|---|---|
| 1 | **§12.10「pip 单包失败 → 尝试降级版本 → 无版本约束安装」** 是补实现还是改文档标注 | 未实现（证据见 §三 第 1 条）；文档已挂账 2026-05-06 至今 | 改文档标注「规划未实现」 | ✅ **已改文档，但结论比原建议更准确**。主控追查失败包下游发现：**"版本调整"并非没做，而是换了承担者**——失败包经 `execution.py:262-266`/`:870` 进入 execution agent 上下文，其工作纪律第 1 条明写「装不全时可用 `run_in_sandbox` 执行 pip install 兜底**或调整版本**」（`:1004`）。故不标"未实现"，改为**如实描述真实形态**（`technical-architecture.md` §12.10 依赖安装块已改写 + 加订正说明块；`product-design-specification.md:421` 表格行同步改通俗中文）。**维持该形态的理由（Maria 认可）**：写死的自动降级比 agent 判断**更危险**——降错版本产出"能跑但结果不对"的复现，正是本系统最该避免的失败形态（装不上会立刻暴露，跑出错结果不会）；且"降到哪个版本"只存在于 pip 解析错误文本里，规则化成本远超收益 |
| 2 | **§12「调用 LLM 前估算 token 数，超限时主动截断/分段」** 同上 | `estimate_tokens` 已定义但**未接入预算**；预算仍按**调用次数**计（证据见 §三 第 2 条） | 改文档标注「规划未实现」+ 与下个 sprint 成本追踪合并立项 | ✅ **Maria 裁：下个 sprint**。与 token / 成本追踪 + 可观测性合并立项（该项本就是 `TODO.md` A 节面试价值最高候选）。单独补一个无消费方的估算函数属过度工程 |
| 3 | **§7.1 MVP 表「CLI 基础入口 P1」这一行** 要不要同步标注 | 见下方「核实补充」 | 随第 7 条一并裁决 | ✅ **Maria 裁：先不建**。§7.1 该行已改为 `~~P1~~ **暂不排期**` + 标注「尚未实现」+ 暂缓理由（`product-design-specification.md:813`）。**措辞按「先」= 暂缓处理、不划删除线**（区别于 PwC 那条的永久摘除），保留日后重启的余地。§5.4 整节命令本体同样保留、不删 |
| 4 | **§6.2 第 1 句末"当前实测覆盖以显卡 / CUDA / 磁盘三项为主"** 这句要不要留 | 本次按诚实性纪律加上（R12） | 留到 S7-08 交付后再删 | ✅ **Maria 裁：先不删除**，维持现状 |
| 5 | **§7.2「复杂错误自动修复 → v2」与已落地 20 轮修复循环的张力** | 2026-07-28 已判为"产品口径待议（'复杂'边界未定义），非硬错" | 维持不动、仅记账 | ✅ **Maria 裁：行**（维持不动只记账）。改它须先定义"什么算复杂错误"，是口径活，不该在收口时顺手做 |

## 四·补充：第 3 条 CLI 的逐行核实结果（2026-07-29 主控应 Maria 要求查实）

**问题一「是不是真没实现？」——答：设计的四条命令确实一条都没有，但"完全没有命令行入口"的说法也不对。**

| 核实项 | 实况 |
|---|---|
| §5.4 设计的 `auto-repro run / plan / code-only / status` | **四条全不存在**，全仓无 `auto-repro` 命令 |
| 现有唯一命令行入口 | `scripts/run_paper.py`（178 行），参数仅 `--arxiv-id` / `--no-save` |
| 它跑多远？ | **会驱动完整主图**（`build_graph()` + `graph.invoke()`，`:146-148`）——**旧文档说"只跑到论文分析"是错的** |
| 为何仍不算产品级入口？ | ①全脚本**零处中断续跑处理**（`interrupt` / `Command` / `resume` 三个关键词均零命中）⇒ 跑到计划审核的人在回路中断处即停、无法续跑；②终端输出只渲染论文元信息与论文分析两节摘要（`_print_paper_meta` / `_print_paper_analysis`），后续节点产出不打印；③docstring 自述"Sprint 1 阶段"、"节点 5/6/7 是占位节点" |
| 旧表述错在哪 | "只跑到论文分析、不含完整链路"源自该脚本 **Sprint 1 时期的 docstring**（当时节点 5/6/7 确为占位符），随七节点全部落地已失效。**本次同步初稿照抄了这句错的**，已于同日订正（`product-design-specification.md:737`） |

**问题二「是打算给这个项目建 CLI 入口吗？」——待 Maria 定，主控建议：不建，把 §7.1 那行从 P1 降级并标注。**
理由：①Web 界面已有完整交互（含人在回路审核页、任务列表与重连），CLI 是同一套能力的**第二个入口**，属重复建设；②本项目的一等权重是秋招作品集的面试价值，CLI 在这个维度上几乎不加分，而 `TODO.md` A 节那两项（评测体系、成本追踪 / 可观测性）才是面试官真会追问的；③真要保留规划，也该把 §7.1 那行补"尚未实现"，否则与 §5.4 节首的"整节未实现"两处打架。

---

# 五、审查后无需改动的重点核对项（宁缺毋滥，说明留档）

- **`product-design-specification.md` §4.2.2 仓库质量评估「兼容性」维度**：措辞本次**不动**（Maria 已转 backlog），状态登记为「**已登记 backlog**」——见 §三 第 3 条。**它同时结清了 2026-07-28 登记的"全局产品文档重评条件①已命中"那条待办**：该条目的状态由"悬空"改为"已登记 backlog"。
- **`product-design-specification.md` §6.1 / §6.3 云服务器模式、§7.2「云服务器远程执行 → v2」、§8.2 v2.0**：与 Maria「远程不做」的裁决一致，且 §7.2 本就把它排除在 MVP 外 ⇒ 不动，**动了反而会制造新的文档-代码不符**。
- **`product-design-specification.md` §9.2 Q6「多 GPU 分布式训练支持程度」**：维持 v2，与 §4.3.3 第 2 条口径一致，不动。
- **`product-design-specification.md` §2.1 用户画像 C（本地无 GPU 笔记本 → 只编码）+ §2.2 场景 3**：S7-08 才让系统据实测**主动**识别并主动提供这条路，当前仍靠用户自己判断 ⇒ 属 C 档，本次不动。
- **`product-design-specification.md` §7.3 验收标准第 3 条「本机环境下能成功执行实验并跑出结果」**：口径本次不改（缩规模场景下"跑出结果"仍成立，结论档位另由缩规模声明约束，属 S7-08 范围）。
- **`technical-architecture.md` §13 实现优先级（阶段 1~4 周表）**：该表是**历史实施计划**（第 1-8 周），不是现状表；把 sp7 的能力写进去属时序错置 ⇒ 不动。§13 阶段 2 表里 `resource_scout 节点` 那行的职责描述同理不动。
- **`technical-architecture.md` §7.2 远程沙箱 Docker（v2）**：与远程不做的裁决无冲突（它本就标 v2），不动。
- **`technical-architecture.md` §12.4 工具层重试表、§12.10 代码执行阶段错误检测表**：2026-07-28 架构师已明确标为"未核实项、明确不猜"，本次同样不猜、不动——**这两张表的逐行实现状况仍是未清的核对面**，建议日后单独排一次核对。
- **`product-design-specification.md` §4.7 语言策略、§9.3 架构问题表、§1.3 其余四条价值主张**：与本次同步无张力。
