# S7-13 端到端真跑验收报告（指标改由 execution agent 汇报）

- **Sprint**：sprint7（S7-13 / 批次 9 / T-S7-9-1）
- **触发原因**：S7-13 交付（`4a7420f`）+ 砍 `source` 字段（`1e2577d`）后，Maria 2026-08-02 **单独授权**真跑验证。本批唯一无法用 mock 证伪的假设是「agent 肯不肯按计划写法填 `group`」。
- **靶**：arXiv:1802.03426（UMAP），跑到 END
- **真跑窗口**：2026-08-02 23:30:12 → 23:35:54，**342.3 秒**（上一轮 2030 秒）
- **thread_id**：`s711-realrun-54a73ffb0923`
- **证据目录**：`/data/myproj/.umap_evidence/20260802-233011/`
- **现场**：`workspace/1802.03426/`（⚠ 会被下次真跑覆盖，关键数据已抄入本报告）
- **上一轮现场**：`workspace/1802.03426_archive_run4_20260801/`

---

## 0. 结论

**本批要验的那条假设成立：agent 确实按计划里的写法填了 `group`，回验链路第一次真正跑起来。**

| 判据 | 上一轮（S7-11 真跑） | 本轮 |
|---|---|---|
| **有 `trend` 的预期条目** | 2 条 | 3 条 |
| **★ 其中真正得到判定的** | **0 条** | **3 条**（2 符合 / 1 不符） |
| 无 `trend` 的条目（planning 侧） | 3 条全未验证 | 2 条全未验证（**预期内，非回归**） |
| `ExecutionResult.metrics`（主指标） | `mean_timing_seconds=44.81`（运行时元数据） | `best_knn_accuracy=0.9766`（科学指标）——🔴 **但这不是本批的功劳，见下** |
| `metrics_groups` | 4 个空组 | **4 组全部非空、含指标值** |
| 修复轮数 | 2 | **0** |
| 耗时 | 2030 秒 | 342 秒 |

> 🔴 **【2026-08-03 订正 —— 主指标那一行归因错误，由测试工程师挖出（F2）】**
> 主控原文暗示「主指标从运行时元数据变回科学指标」是本批修好的，**归因错了**。实测：agent 自报的 24 条**全部带 `group`** ⇒ `reported_main` 为 `{}` ⇒ **主指标合并分支本轮根本没执行**；`best_knn_accuracy` 是 `_extract_metrics_block`（**本批一字未改**）取最后一块取到的——主控独立复算，对同一份 `round_0.log` 单跑该函数，返回值与最终 `metrics` **逐字节相同**。
> ⇒ **真实归因**：本轮 coding 生成的代码，最后一步打印的那块**恰好含科学指标**（上一轮最后一块是汇总脚本的元数据）。**是产物变了，不是系统修好了。** P-70 登记的残留**依然全额存在**。

> ⚠ **对比口径须精确**：两轮的 `reproduction_plan` **不是同一份**（每次由 LLM 重新生成，本轮 11 步 / 3 条 trend，上轮 12 步 / 2 条 trend）。因此**不能表述为「5 条全未验证变成 2 条符合」**——那混入了"计划本身变了"这个因素。**可比的口径是「有 trend 的条目判定率 0/2 → 3/3」。**

---

## 1. 回验结果（本批的核心观测量）

`report.md`「计划目标回验」节实际渲染：

| # | 计划预期（摘要） | 有无 `trend` | 回验结果 |
|---|---|---|---|
| 0 | UMAP 嵌入应形成清晰簇结构，定性可视化与论文一致 | ❌ 无 | ⚠️ 未验证 |
| 1 | 小数据集 k-NN 评估，UMAP 与 t-SNE / LargeVis 同量级 | ✅ `UMAP > PCA` | **✅ 符合** |
| 2 | COIL-20 等流形数据上全局关系保留应更优 | ❌ 无 | ⚠️ 未验证 |
| 3 | 运行效率上整体耗时趋势应优于较慢的非线性方法 | ✅ `t-SNE > UMAP` | **✅ 符合** |
| 4 | 增大 k 时 UMAP 应更能保持性能稳定 | ✅ `UMAP > t-SNE` | **❌ 不符** |

> 🔴 **【2026-08-03 撤回并订正 —— 本节原结论错误，由测试工程师独立验收挖出（F1，候选 BUG-S7-13-01）】**
> 原文曾写：「第 4 条『不符』是本轮最有价值的产出，这是系统第一次敢于判定『论文这条我没复现出来』……三条判定与磁盘数据逐条对得上，**无误判**」。
> **该结论站不住，已撤回。** 主控当时只复算了"给定这组数字，比较结果算得对不对"，**没有质疑这组数字本身是怎么被选出来的**。

**真相：那三条判定是任意 tie-break 的产物，不是复现结论。**

agent 实际自报 **24 条**指标（4 方法 × 3 数据集 × 2 指标），而 `metrics_groups` 的结构是 `{组名: {指标名: 单值}}` —— **二维容器装不下三维数据**，必然坍缩。生产实现的坍缩规则是"同名异值保留**首次**出现值"（日志留痕 16 处），而"首次"取决于 agent 数组里谁排前面，**与实验本身无关**。

把 tie-break 换成同样任意的"保留**末次**"，**同一份自报数据、判定结果翻转**（主控独立复算）：

```
保留首次（生产实现）：UMAP k-NN 0.6201  vs  t-SNE 0.7026  ⇒ 第4条 符合 / 第5条 不符
保留末次（同样任意）：UMAP k-NN 0.9766  vs  t-SNE 0.9727  ⇒ 第4条 不符 / 第5条 符合
```

UMAP 的 k-NN 准确率实际被报了 **3 条**（`0.6201` / `0.8175` / `0.9766`，对应三个数据集），生产实现只留了第一条。

🔴 **更严重的一层**：被选中的第一条来自 `COIL-20`，而 `round_0.log` 第 4 步原文显示——

```
[step#4] python scripts/prepare_data.py --dataset COIL-20 ...
[stdout] downloading Olivetti faces from https://ndownloader.figshare.com/files/5976027
         {"dataset": "COIL-20", "n_samples": …}
```

**代码声称准备 COIL-20，实际下载的是 Olivetti 人脸，并把产物标注为 `"dataset": "COIL-20"`。** ⇒ 那个「不符」判定所依据的数字，**来自一份数据集张冠李戴的实验**；且报告表格**无任何数据集标注**，读者无从察觉。

**性质**：这是**缺陷一（`<METRICS>` 只取最后一块）的同型病**——把"最后一个赢"换成了"第一个赢"，**任意性没有消除，只是换了个方向**。dev-plan §60.4 曾判定"新方案下根本不发生撞名"，**实测发生 16 处**。

**本批不修**（已超出 S7-13 范围，且修法必然改数据结构），登记为 **候选 BUG-S7-13-01**，须 Maria 拍板。**对外表述纪律**：在此之前，**不得把本轮任何一条回验判定（符合 / 不符）当作复现结论引用**；可引用的只有「回验链路已打通、有 trend 的条目不再一律未验证」这一机制性事实。

---

## 2. agent 的服从度（本批唯一无法 mock 证伪的假设）

`metrics_groups` 实际内容：

```json
{
  "PCA":                 {"k-NN classifier accuracy": 0.2699, "runtime": 0.1207},
  "UMAP":                {"k-NN classifier accuracy": 0.6201, "runtime": 0.6467},
  "t-SNE":               {"k-NN classifier accuracy": 0.7026, "runtime": 0.7520},
  "laplacian_eigenmaps": {"k-NN classifier accuracy": 0.3300, "runtime": 0.1199}
}
```

- **★ `t-SNE` 带连字符大写** —— 正是此前 `_normalize_group_key("t-SNE")="t_sne"` 与目录 `baselines/tsne`→`baselines_tsne` **匹配不上**的那个写法。agent 按计划原文填了，**名字对不上的问题被绕过而非修补**，方案成立。
- **指标名同样按计划写法**：`k-NN classifier accuracy` / `runtime`（产物文件里实际字段是 `knn_accuracy` / `timing_seconds`）⇒ 组名与指标名两条约束**同时生效**。
- ⚠ **但服从不是 100%**：**`laplacian_eigenmaps` 用的是目录名风格**（计划预期里未提及该方法，agent 退回产物原名——这与 prompt 里"expected_results 没提到的指标按产物原名填"一致，**属合规**）。**本轮 4 组中 3 组按计划写法、1 组按产物原名，均无违规**；但**不得据此宣称「agent 一定会照做」**——单次真跑、单靶、单模型。

**去重逻辑实际触发**：日志原文 `agent 自报指标：16 处同名异值重复，保留首次出现值（PCA::k-NN classifier accuracy; PCA::runtime; UMAP::k-NN classifier accuracy; UMAP::runtime; laplacian_eigenmaps::k-NN classifier accuracy）` ⇒ agent 按数据集逐条报了（4 方法 × 多数据集），去重按"保留首次"收敛为每组一条。**该行为如实留痕，非静默丢弃。**

---

## 3. 执行与判定

```
步骤对账：planned=11  planned_actionable=11  executed=11  completed=11
          unexecuted_steps=[]  attribution_unavailable=False
          extra_commands=['ls outputs/eval', 'ls outputs/report',
                          'sed -n 1,40p outputs/eval/knn_results.csv', 'echo ---',
                          'sed -n 1,120p outputs/report/summary.json']
round_0.log：16 条命令（11 计划步骤 + 5 探查）/ 非 0 退出 0 条 / 修复轮 0
产物：outputs/ 14 MB / 46 文件
```

- **S7-11 的完成度判定继续正确工作**：11/11 全部完成，无"少跑步骤却判成功"。
- **一次跑通、零修复轮** —— 与上一轮（2 轮修复、8→2→0 失败收敛）不同，本轮计划规模更小（11 步 vs 12 步）且未撞上数据集下载失败。
- **S7-12 的元字符拦截实际触发一次**：日志 `run_in_sandbox 工具拒绝内联写码命令（约束 C 硬拦截）：python -c "import json, pathlib; ..."` ⇒ 该防线在真实链路上生效，agent 随后改用 `sed` 查看文件（见 `extra_commands`），**未被卡死**。

---

## 4. 🔴 主控操作失误（自认，同一个坑的第二次）

**跑前前置①「计划全文落盘」再次落成 2 字节空对象。**

上一轮（2026-08-01）已犯同样的错并写进 `docs/TODO.md`「真跑入口需固化」节，明确列出根因（取值点应从 interrupt payload 取、键名是 `execution_steps`）。**但本轮启动前，主控只修了脚本里的追踪项目名（改成时间戳自动生成），未修取值点与键名** ⇒ **同一个坑连踩两次**。

- **补救**：仍靠 checkpoint db 落在持久目录，`sqlite3.backup()` 只读快照补取成功（8817 字节 / 11 步全可执行）。
- **教训**：「记进 TODO」不等于「修好了」。TODO 里那条待办列的是**四项**前置，主控只做了其中一项就开跑。⇒ 该条待办的性质应从"待计划"升级为"已两次实际造成证据落空"。

---

## 5. 已知限制与未做项

1. **单次真跑、单靶、单模型**（`azure/openai/gpt-5.4`），§2 的服从度结论**不构成统计证据**。
2. **两轮计划不同**，§0 的对比须按"有 trend 条目的判定率"口径读，不可按绝对条数读。
3. **2 条无 `trend` 的预期本轮仍未验证** —— 成因在 planning 侧（LLM 未产出可比较结构），**S7-13 范围外，看到它们没变属预期非回归**。
4. **`_extract_metrics_block`「取最后一块」一字未改**（P-70 残留）：本轮主指标之所以正确，是因为 agent 报了主实验指标补上；**若它不报，`metrics` 仍会只剩运行时元数据**。
5. **本批正式测试为零**（开发自测脚本落 `/tmp` 未入仓）—— 回归 2494 改前改后一致**只证明零退化、不证明防线在守**。补测已于本报告落盘同期派出，结果见 `tests/` 与对应测试报告。
6. **未跑 `-m e2e` 套件**；本轮不改代码，回归结论沿用 `1e2577d` 的 2494 passed。

---

## 6. 现场取证索引

| 项 | 值 |
|---|---|
| 报告 | `workspace/1802.03426/report.md` |
| 日志 | `exec_logs/round_0.log`（仅一轮） |
| 计划 | `/data/myproj/.umap_evidence/20260802-233011/reproduction_plan.json`（8817 字节，事后补取） |
| 结果摘要 | 同目录 `final_summary.json`（1019 字节） |
| checkpoint | 同目录 `realrun.db` |
| 追踪项目 | `s711-realrun-20260802-233011`（本轮起按启动时间戳自动生成，与证据目录同名一一对应） |
