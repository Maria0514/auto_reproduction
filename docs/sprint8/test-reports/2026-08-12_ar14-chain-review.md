# 测试执行报告 - ar14-chain-review（`P-S8-63` 独立复核）

- **日期**：2026-08-12 22:47 PDT
- **执行人**：@测试工程师代理
- **Sprint**：sprint8
- **触发原因**：独立复核主控登记的 `P-S8-63`「`AR-S8-14` 因果链断在第三跳」，**复核立场为默认倾向推翻**
- **commit**：`d2f804a`
- **权限口径**：只读复核。本次**唯一**写入仓库的文件就是本报告；`core/**`、`tests/**`、`docs/sprint8/{prd,architecture,dev-plan}.md`、`docs/TODO.md`、`docs/MEMORY.md` 逐一零字节改动（交付前 `git status --porcelain` 自证）。**不勾任何检查点、不销任何 TODO 账**。

---

## 0. 判定

# 🔴 `结论成立`

主控的结论**成立**，且**被我独立复现的证据比它自己给的更强**：我另造的两个构造（丁 / 戊）表明，`AR-S8-14` 的挡板不只是"第三跳没接上"，而是**整条挡板对作弊方是可选的**。

同时，我按要求做的"反向假设优先"三问**逐条不成立**：`_decide_conclusion` 的三处实现细节（`not any` / 取顶层 `evidence` / 不读逐条结论）**逐条忠实于文档原文，且逐条是两个可选实现里更严的那个**。⇒ **断的是文档的链条，不是实现写错了。**

---

## 1. 执行范围

- **命令**：三个自写探针，`.venv/bin/python /home/yujingm/.claude/jobs/134ae0a1/tmp/review_ar14/probe{,2,3}.py`
  （从零编写，未复用主控脚本；临时目录在仓库外，仓库零残留）
- **被测物**（纯函数直调，零 LLM / 零网络 / 零 deepxiv 配额）：
  `core/nodes/execution.py` 的 `_verify_evidence` / `_evidence_text` / `_evidence_key` / `_build_evidence_ledger` / `_decide_conclusion` / `_completion_insufficient`
- **磁盘现场**：自造 `code_out/outputs/summary.json`（真实存在、内容含 `0.61`）
- **共同输入**：`baseline_results = {"knn_accuracy": 0.95}`、agent 报 `{metric: "knn_accuracy", value: "0.61"}` 并自称「印证上了」、`conclusion_level = "复现成功"`、`exit_ok = True`、`recon = {planned_actionable: 3, completed: 3}`（`_completion_insufficient` 实测为 `False`）
- **是否包含 e2e**：**否**。本次零凭证、零外部调用。

---

## 2. 结果摘要

| 情形 | 档位物证（顶层 `evidence`） | 实测 `level` | 逐条 `verdict` |
|---|---|---|---|
| **丙** | 只有编低的论文值 | `仅代码跑通` ✅ | `无法核实` |
| **甲** | 编低的论文值 + 一条过验真产物 | 🔴 **`复现成功`** | `无法核实` |
| **乙** | 只放过验真产物（编低值只在逐条预期里） | 🔴 **`复现成功`** | `无法核实` |
| **丁**（我加的） | 只放过验真产物，**编低的基准根本不写进汇报** | 🔴 **`复现成功`** | 🔴 **`印证上了`** |
| **戊**（我加的） | 同丁，且 `baseline_results = {}` | 🔴 **`复现成功`** | 🔴 **`印证上了`** |

单条验钞自检（证明前两跳确实有落地物）：
```
编低的论文值 -> (False, '它写的数值和论文分析里记下的这个指标对不上')
真产物       -> (True, '')
```

- 通过：5 个构造全部按预期跑出结论（**"按预期"指探针跑通，不指档位合理**）
- 失败：0（本次不是回归跑）
- 跳过：0
- 警告：1 类（`LangChainPendingDeprecationWarning`，来自 `langgraph.checkpoint.serde`，与本次无关，属项目级长期 warning，历次报告已见）
- 总耗时：< 10 秒 / 探针

---

## 3. 逐条判定

### 3.1 (A) 独立复现 —— 主控给的三个构造**逐个复现成功**，数字逐字一致

丙 = `仅代码跑通`、甲 = `复现成功`、乙 = `复现成功`。**与 `P-S8-63` 登记的一字不差。**

机理也复现一致：
- 第一跳有落地物 —— `_verify_evidence` 论文值侧第⑦重把 `"0.61"` 与记录值 `0.95` 判不成立（`execution.py:2496`）；
- 第二跳有落地物 —— `_decide_conclusion` 的 `if not ids or not all(ok_by_id...)` 把该条预期落 `无法核实`（`execution.py:2745-2746`）；
- **第三跳没有落地物** —— `level` 的三个封顶输入是 `exit_ok` / `_completion_insufficient(recon)` / `not any(ok of level_ids)`（`execution.py:2768-2774`），**`goal_checks` 的裁决一个字都没进去**。

### 3.2 (B) 反向假设优先：**不是实现写错了**（三问逐条驳回）

| 反向假设 | 判定 | 把柄 |
|---|---|---|
| 封顶③ 的 `not any` 是否忠实于原文？ | ✅ **忠实** | PRD `:205` 逐字「agent 所报档位的支撑物证**一条都不成立**」；架构 `§16.3.2` 第 2 条逐字「档位的支撑物证**若全不成立**」。两处都是"全灭才封顶"，`not any` 是它唯一的正确译法 |
| 封顶③ 的输入取顶层 `evidence`（`level_evidence_ids`）而不是全部台账，有文档依据吗？取全部台账会不会才对？ | ✅ **有依据，且现取法严格更强** | 架构 `§2` 形态图**逐字注**：`"level_evidence_ids": ["E1"],  // 支撑**档位本身**的物证引用（封顶 3 判据的输入）`。**并且我实测了"取全部台账"这个反案**：档位物证只有编低论文值、但结果块里挂一条过验真产物时——现行实现落 `仅代码跑通`，改成全台账则 `not any([True, False]) = False` ⇒ **封顶不命中、落「复现成功」**。⇒ **取全部台账是把闸松开，不是收紧。反向假设在这里方向就反了。** |
| `goal_checks` 不参与档位，是主控读窄了吗？ | ⚠ **措辞略宽，结论正确** | `AR-S8-04` 的字面是「只读 `level` + 数封顶，**不读证据形态、不解析证据语义**」——**字面确实没写"不读逐条结论"**，主控这半句是对「只读 level + 数封顶」的转述。**但门是另一条关的**：架构 `§16.3.2` 第 2 条逐字「🔴 **不新增第四条封顶**」+「**开发不得另写一条"论文值对不上则降档"的分支**」。⇒ **主控的机制②归因略糙，结论不受影响。** |

⇒ **实现无可指摘。真正错的是架构 `§16.3.2` 第 2 条那句论据**：「既有两个出口**已完全覆盖**」——甲 / 乙两个构造直接证否（出口②「档位的支撑物证若全不成立」在还剩一条过验时**根本不触发**）。

**顺带挖出一处更深的论证瑕疵（主控未提）**：`§16.3.2` 第 2 条把自己的取向挂靠在 `§2.5.6`（「标准缺失时的档位语义是既有封顶的推论，不是新增第四条封顶」）上，称"逐字同源"。**但两者不同源**：`§2.5.6` 的推理成立，是因为它那个场景下「没有成立的支撑物证」**为真**，封顶③ 真的接得住；而 `AR-S8-14` 场景下那个前提**为假**（还剩一条过验）。⇒ **同一个类比在一处成立、在另一处不成立，架构照搬了结论没照搬前提。**

### 3.3 (C) 主动找"能把第三跳接上"的机制 —— 全面扫过，**一处都没有**

| 扫描面 | 结论 | 依据 |
|---|---|---|
| `core/nodes/execution.py` 现状 | 无 | `level_evidence_ids` **全仓只有一个生产者、零消费者**（`grep -rn level_evidence_ids core ui tests` 仅命中 `execution.py:2780`） |
| `core/nodes/reporting.py` | 无 | 只有渲染。批次 3 的 `T-S8-3-3` 明令**判定段整体删除**，`CP-3.3-4` 是一条**负向静态断言**：`_assemble_conclusion` 函数体内不得出现任何 `level = ...` 的条件赋值 |
| `core/plan_checks.py`（W6 / 护栏 3） | 无 | 管的是**计划文本引没引用论文事实**，与 agent 汇报的论文值无关；且**只产警示、不阻断**（架构 `§15.1`）。dev-plan `§0.5` 另有一句写死：W6「**不参与档位判定**」 |
| `honesty_audit` 链 | 无，且是**已登记的反向** | PRD `§4.9.3 C` 逐字：造假审计一票降档**撤销** ⇒「**造假这条线上不再有任何自动机械闸**」，登记为 `R-S8-14`。`AC-S8-17①` 甚至**要求用一条用例证明**「审计命中 + 判复现成功 + 物证过验 ⇒ 档位仍为复现成功」 |
| `ui/pages/result_report.py` | 无 | `:134` 只读 `conclusion.get("level")` 用于分卡 |
| **未实现的 `T-S8-2-7`** | 无，**结构上不可能** | 触发条件写死为 `exit_ok ∧ category == NONE ∧ level == "仅代码跑通"` ⇒ 它**只在档位已经掉到「仅代码跑通」之后才动**，无法把 `复现成功` 拉下来 |
| **未实现的 `T-S8-2-8`** | 无，反向放大 | `success = level in {"复现成功","部分复现"}` ⇒ 甲 / 乙 / 丁 / 戊 一律 `success=True` ⇒ **直通报告环节，不回编码环节** |
| **未实现的 `T-S8-2-9`（提示词）** | 不算机制 | 只能要求 agent「引用论文值时须写明指标名」；本项目实测服从率约 75%，且**存心作弊的 agent 没有服从动机**（正是构造丁的成因） |
| **未实现的 `T-S8-2-10b` / `2-11` / `2-12`** | 无 | `2-11` 只是接线（步骤 4.75 / 4.8 插桩 + 废 S7-13 门控）；`2-10b` 是块收编；`2-12` 是断言同步 + 收口门 |
| **批次 3 报告侧全部任务** | 只到"标注"为止 | `T-S8-3-6` 的扩围逐字是「**引用到 `ok=false` 记录的条目显著标注**」；`T-S8-3-4` 更要求文案**明说「以下写法不影响本次结论档位」**、把 `:612-613` 的「已据此降档」改掉，因为**那句已经变成假话** |
| 批次 4 / 5 | 无 | 回归对账 + 真跑取证，无判定逻辑 |

🔴 **反而扫出一条对结论杀伤更大的事实：第三跳在 Sprint 8 之前是有落地物的，是本次改造把它拆掉了。**
`core/nodes/reporting.py:315-322`（**现行代码，未删**）：

```python
elif (
    goal_checks
    and all(check.get("verdict") == _VERDICT_MATCH for check in goal_checks)
    and not annotations
):
    level = "science"
else:
    level = "engineering"
```

⇒ **旧三档制下"任一条预期没对上 ⇒ 从最高档掉下来"是有代码的**（`all(...)`）。四档制把判定搬进 execution 后，这条链路**没有对应物被搬过去**。⇒ `P-S8-63` 不只是"文档写了个没落地的承诺"，它同时是**一次相对 Sprint 8 之前的防护净退化**，而且退化点正好落在防作弊面上。**这一条建议补进 `P-S8-63` 的证据栏。**

### 3.4 (D) 危害口径有没有被夸大 —— **没有，反而偏保守**

- 「只标注、未堵死」这个说法**如实**：甲 / 乙两种情形下报告确实会同时印出「复现成功」+ 一条「无法核实」+ 台账里一条 `ok=false`（`T-S8-3-6` 还要求对它**显著标注**）。**标注是真的，堵死是假的**——主控两半句各自准确。
- 「全靠人眼发现」**如实**：`AC-S8-17①` 已经确立"矛盾并存"是本 Sprint 的**预期形态**（审计命中 + 复现成功 并存），产品上已接受这类自相矛盾报告，没有任何机械闸兜。
- 「对外表述撞 `R-S8-01`」**如实且必要**：架构 `§16.3.2` 的「⇒ 拿不到它想要的那一档」+「**零新机制**」与 `CP-2.5-10` 的「⇒ **拿不到「复现成功」**」，是三处**可被单条构造证否**的强口径。
- 🔴 **口径归属要更精确一点（主控没分）**：**PRD 侧没有说错**。`R-S8-26` 的缓解栏写的是「`AC-S8-06` 第⑥⑦重 + `AC-S8-15④`」——**它从头到尾只承诺了验钞层，没承诺封顶**。⇒ **需要收窄的是架构 `§16.3.2` 第 2 条 / `§10` 的 `AR-S8-14` 行 / dev-plan `CP-2.5-10` 这三处，不是 PRD。** 这一条能把甲案的改动面再缩小一圈。
- ⇒ **不构成过度渲染。** 若说有偏差，是**偏保守**——见下节。

---

## 4. 🔴 主控没测到的两个构造（本次复核的独立增量）

`P-S8-63` 的三个构造都**默认作弊的 agent 会把编低的论文值写进汇报**。我把这个前提去掉后：

| 构造 | 做法 | 实测 |
|---|---|---|
| **丁** | agent 根本不提交论文值物证，直接拿一条过验的真产物去撑「数值与论文报告对上」这条预期 | `level = 复现成功`，逐条 `verdict = 印证上了`，**台账里一条 `ok=false` 都没有** |
| **戊** | 同丁，且 `baseline_results = {}`（上下文压根没给论文值） | 同上：`复现成功` + `印证上了` |

**这两条的分量大于甲 / 乙**：

1. **它把 `AR-S8-14` 的挡板性质从"漏了一跳"改判为"整条对作弊方可选"。** 七重里的第⑥⑦重**只在 agent 主动交出带 `metric` 的物证时才启动**；不交，就一条都不触发 ⇒ **既没有 `ok=false`，也没有「无法核实」，报告上连主控说的那点自相矛盾都不会出现**。⇒ 主控的危害描述是**保守**的，不是夸张的。
2. **它直接决定了三条候选路的性价比**（见 §5）：乙 / 丙都是在"第三跳"那一段接线，而丁 / 戊**根本走不到第三跳**——它们在第一跳就绕过去了。⇒ **付出乙或丙的代价，买到的保护只覆盖"既作弊又如实申报自己作弊"的 agent。**
3. **顺带暴露一处 AC 与实现的射程差**（口径级，非 bug，建议交架构师定夺）：`AC-S8-06⑦` 的字面是「上下文没给论文报告值时，**任何「与论文对上了」的声称**一律不成立」——"声称"是**逐条预期层**的词；而实现把它落在**物证条目层**（只对带 `metric` 的条目生效，`execution.py:2469-2470`）。构造戊 就是这个差的活体：上下文没给论文值，「与论文对上了」的声称照样成立。**实现的窄读法与 `⑧任一不过则该条判断不成立` 自洽**，所以我不判它是实现 bug，但**两份文档对同一句话的射程不同，须显式裁一次**。

---

## 5. 三条候选路的测试工程视角评判（判"成立"后的附加交付）

> 结论先给：**我也倾向甲，但理由与主控不同，且比主控的理由硬。** 主控的理由是"为 `AR-S8-14` 去松 `AR-S8-04` 不划算"（一个权衡判断）；我的理由是**乙和丙都堵不住丁 / 戊，买不到它们标价的那个东西**（一个事实判断）。

| 候选 | 回归面 | 造新假绿的风险 | 我的判定 |
|---|---|---|---|
| **甲·收窄表述**（零代码） | **最小**：零生产改动、零用例翻转；文档改动面还能比主控写的更小——**PRD 不用动**（§3.4 末条） | **零**（不动代码就造不出假绿） | ✅ **推荐**。另需同批把 `CP-2.5-10` 的验收语义与 `R-S8-01` 局限表一起收窄 |
| **乙·`not any` ⇒ `not all`** | **大** | 🔴🔴 **最高，且是立刻发作的新假绿**——**实测算术**：`not all([]) = False`。⇒ **「汇报缺失」这一格当场不再被封顶**，起点取最高档直通 `复现成功`。而架构 `§1.4` 第三行把这一格**写死**为「仅代码跑通」，理由逐字是"等价于支撑物证一条都不成立"；`V4` 用例守的就是它。⇒ 照字面改会**一次打掉一条已写死的产品语义**，必须写成 `not all(...) or not level_ids` 才勉强等价 | ❌ **反对**。除上述外，"任一物证笔误即整轮压档"的误伤面在一个服从率约 75% 的系统里不可控 |
| **丙·加第四条封顶** | **中**：只加一条 `_LEVELS` 下标，`取 max` 的"只压不抬"是结构性保证、不会被这条破坏 | **低**（只压不抬） | ⚠ **技术上比架构说的干净，但仍不推荐**。三点须如实登记给 Maria：<br>① 它**不是"自造新规则"**——PRD `§4.5.2` 对「部分复现」的定义**逐字就是**"跑通、产物齐，但**部分**预期没印证上或核实不了"，丙 只是把第一层的语义定义抄成代码；<br>② 它**不违反 `AC-S8-08②`**——禁的是按**证据内容形态**（数值 / 趋势 / 定性）分支，读一个三态裁决不是形态分支；<br>③ 但它**正面撞 `AR-S8-04` 的字面**（"只读 level + 数封顶"）与 `§16.3.2`「不新增第四条封顶」，**属两条红线打架，必须 Maria 裁，不能开发自己接**（`MEMORY §3.12` 末句）。<br>🔴 **而即便裁给丙，丁 / 戊 仍然过**——这才是不推荐它的决定性理由 |

**给 Maria 的一句话**：这三条路里，只有甲**没有标错价**。乙和丙的标价是"堵住编低基准"，而实测证明它们只堵住"编低且申报"的那一半；真要堵死 `AR-S8-14`，需要的是**一条"达标线引用了论文 ⇒ 该条预期必须挂一条论文值物证"的要求**，那是一次**新的产品决策**（且直接踩"按证据形态分支"的边界），**不属于本次收口的范围**。

---

## 6. 失败排查

**无失败用例。** 本次为只读复核，未跑 pytest 套件（探针是纯函数直调）。

需要显式登记的**判断过程**（避免被读成"跑了三下就下结论"）：

1. 先按"实现写错了"的方向查了三问，**逐条查到文档原文并逐条驳回**（§3.2），其中"取全部台账"那一问我**实测了反案**才敢说方向反了——只靠读文档推不出"改了会更松"。
2. 再按"文档链条断了"的方向，**扫了 9 个面**（含 6 个尚未实现的任务规格），确认没有任何一处接得上（§3.3）。
3. 最后才去挑主控构造的**前提**（"作弊的 agent 会申报自己编的基准"），挑出丁 / 戊 —— **这一步是本次唯一真正的增量**，也是判"成立"之后仍值得交付的部分。

---

## 7. 后续动作 / 遗留风险

1. 🔴 **建议补进 `P-S8-63` 证据栏（交主控落盘，我不动 dev-plan）**：
   - ① 构造 **丁 / 戊**（挡板对作弊方可选，报告上连矛盾都不会印）；
   - ② **第三跳在 Sprint 8 之前是有落地物的**（`reporting.py:315-322` 的 `all(...)`），本次改造把它拆掉且没搬过去 ⇒ 这是一次**净退化**，不只是"承诺没兑现"；
   - ③ 口径归属订正：**PRD `R-S8-26` 没有写错**，需要收窄的只有架构 `§16.3.2` 第 2 条 / `§10` `AR-S8-14` 行 / dev-plan `CP-2.5-10` **三处**；
   - ④ 若走乙案，**必须同批处理 `not all([]) == False`**，否则当场打掉架构 `§1.4` 第三行。
2. ⚠ **新开一条待裁项（建议编号交主控）**：`AC-S8-06⑦` 的射程——"任何**声称**"（逐条预期层） vs 实现的"带 `metric` 的**物证条目**"（条目层）。**须架构师裁一次**，否则测试工程师落 `AC-S8-06⑦` 的正式用例时，按 PRD 字面写必红、按实现写等于把 AC 改窄（`MEMORY §3.11` 那一族的典型入口）。
3. 🔴 **遗留风险（本次顺带核出，与 `P-S8-63` 无关但同批交付）**：`T-S8-2-5` / `T-S8-2-6` 两次交付**只有生产代码、`tests/` 零新增**（`git log --stat` 自证），全仓对 `_verify_evidence` / `_build_evidence_ledger` / `_decide_conclusion` / `_LEVELS` 的**测试引用为零**。这**符合** dev-plan `§0.3` 第 6 条（"新增正式测试不在开发侧，交测试工程师"），**不是违规**；但意味着**七重验钞与四档封顶目前在仓库里没有任何回归守门**。⇒ 一旦 `P-S8-63` 裁定落地要动封顶③，**没有基线用例能证明"只改了该改的那一格"**。建议在批次 2 收口门（`T-S8-2-12`）之前，由我方补一批正式用例，**至少覆盖**：三条封顶各自的边界、`not any([])`（汇报缺失那一格）、论文值侧第⑥⑦重、台账去重与 id 分配序。
4. **下一次跑测试的触发条件**：Maria 对甲 / 乙 / 丙 拍板之后 —— 若裁乙或丙，**必须先有第 3 条那批基线用例**再动实现。

---

## 8. 环境与自证

- Python：`.venv/bin/python`（MEMORY §2）
- 临时脚本落点：`/home/yujingm/.claude/jobs/134ae0a1/tmp/review_ar14/`（仓库外，符合本轮硬边界；探针造的磁盘现场也全在该目录下的 `tempfile.mkdtemp` 里，**未往 `/tmp` 或工作区写一个字节** —— 同 `MEMORY §六`「断言某副作用不该发生的落点必须隔离」的同款取向）
- 本报告记录的**可老化基线**（`MEMORY` D-1 纪律）：`commit d2f804a`、`git log --stat` 的"两次交付零测试文件"、`reporting.py:315-322` 的 `all(...)` 分支 —— **均为 2026-08-12 22:47 PDT 当刻现查**；批次 3 的 `T-S8-3-3` 一旦交付，`reporting.py` 那处即消失，届时本报告 §3.3 的那段代码引用**将失效但结论不变**（结论是"它曾经存在过"）。
