# 测试执行报告 - s2-13-acceptance（S2-13 用户提供仓库统一抓取分析 + 同口径评分通道 独立验收）

- **日期**：2026-06-13
- **执行人**：@测试工程师代理
- **Sprint**：sprint2
- **触发原因**：S2-13 实现完成，做独立验收（逐条复核 AC-S2-20~26 + 补边界 + Playwright UI e2e），不轻信开发自报的 27 用例
- **commit**：92d90d5（S2-13 实现改动未提交，在此工作区）

## 验收结论：PASS

S2-13「用户提供仓库统一抓取分析 + 同口径评分通道」实现与 PRD §2.13 / 架构 §2.13.1~2.13.12 完全一致，AC-S2-20~26 全部命中（逐条 python 探针 / AST 实证，非仅看开发用例）。两个一线缺陷已修复且经独立复核：① revise/对话贴链接经模型判断后真正入候选；② 切换仓库后 quality_score 为同口径真实非 0 值。**零生产 BUG**。全量非 e2e 回归 538 → 559 passed / 0 failed（净增 21 = 19 边界 + 2 browser e2e）。

## 执行范围
- 命令：
  - `.venv/bin/pytest -q -m "not e2e" --ignore=tests/test_paper_intake.py`（全量回归守门）
  - `.venv/bin/pytest tests/test_sprint2_s2_13.py tests/test_sprint2_s2_13_boundary.py -q`（mock 单元）
  - `.venv/bin/pytest tests/test_sprint2_s2_13_e2e.py -v -m browser`（Playwright UI e2e）
  - 多段 python 探针（importlib 直驱 `_merge_user_repos_from_tools` / `_map_planning_result` / `planning()` / `_switch_selected_repo` / `_normalize_repo_url` / `_await_phase`，含工具调用计数 instrument）
- 覆盖用例：
  - 开发自测 `tests/test_sprint2_s2_13.py`（27 用例，复核通过）
  - 新增 `tests/test_sprint2_s2_13_boundary.py`（19 用例，测试工程师补强）
  - 新增 `tests/test_sprint2_s2_13_e2e.py`（2 Playwright browser 用例）+ harness `tests/e2e_harnesses/_e2e_s2_13_app.py`
- 是否包含 e2e：browser e2e 是（默认可跑，chromium 已装）；真实 LLM/网络 e2e（Task D）**skip**——凭证 LLM_API_KEY + DEEPXIV_TOKEN 均缺失（不伪造）

## 逐条 AC 复核（独立实证）

| AC | 结论 | 实证方式 |
|----|------|---------|
| **AC-S2-20** 对话/revise 贴链接经模型判断纳入候选 | PASS | 探针直构含成功 `git_clone_and_analyze` ToolMessage 的 react_messages → `_merge_user_repos_from_tools` merged=True、`repos` 含规范化 URL、`source=="user_provided"`、`code_strategy` from_scratch→use_repo；无正则强制识别（无工具调用历史时 merged=False） |
| **AC-S2-21** 切换仓库真实质量分非 0 + 命中不重复 clone | PASS | 全 `planning()` 端到端探针：pending_url + mock 工具成功 + 模型给 0.81 → selected.quality_score=0.81（≠0.0）、local_path 非空、被选中、在 repos 中；**架构师建议的「工具调用次数==0」断言**：instrument `git_clone_and_analyze` 工厂，命中既有候选路径（`_switch_selected_repo` + merge 命中）实测调用 0 次、无重复卡片 |
| **AC-S2-22** 同口径可比较 | PASS | `planning` / `resource_scout` / `_repo_scoring` 三处 `REPO_QUALITY_SCORING_SECTION` `is` 同一对象；**resource_scout prompt 字节等价实证**：从两节点 system prompt body 中按索引切出该段，`.encode()` 三方字节相等；两路径同走 `resource_scout._build_repo_info`，字段集合实测完全一致（12 字段）、quality_score ∈ [0,1] |
| **AC-S2-23** stars/forks 留空不报错 | PASS | merge 后 RepoInfo stars/forks 恒 None；browser e2e（ok 变体）实测候选卡片渲染「—」占位、页面不崩；探针确认评分不依赖 stars/forks |
| **AC-S2-24** 模型不调工具时 repos 不变 | PASS | react_messages 仅普通 AIMessage（无 git_clone ToolMessage）→ merged=False、tool_attempted=False、repos 长度不变 |
| **AC-S2-25** clone 失败强制重填 | PASS | **强制断言点 payload `switch_repo_failed=True`**：`planning()` 端到端三类失败参数化（PermanentError/TransientError/越界）→ payload.switch_repo_failed=True、URL 不入 repos、不写 0.0 占位、selected 不变、node_errors degraded、不抛异常；入口 a（无 pending_url）失败**不置** switch_failed、按无新仓库继续；成功后清标记、re-submit 清旧标记 |
| **AC-S2-26** awaiting 轮询不破坏 | PASS | `_await_phase` 纯函数探针：switch_repo 失败后 revise_count 前进 → 返回 `to_review`（不卡 waiting）；未前进 → waiting |

## 治理范式核查
- **BUG-S1-02（无 str(dict)，全程 JSON）**：PASS。S2-13 路径全文 grep `str(dict`/`str({` 仅命中 git_tools.py 一句**禁止性 docstring**；工具产出 `_serialize_tool_result`（json.dumps），合并解析复用 resource_scout `_parse_tool_content`（json.loads），全程合法 JSON。
- **BUG-S1-03（3 参签名 + 失败 ToolMessage 过滤）**：PASS。`_map_planning_result(result, state, react_messages)` 实测 3 参；`_merge_user_repos_from_tools` 过滤 `Error in `/`tool ` 前缀、`success==False`、缺 `local_path` 三类失败（探针逐一验证均 merged=False）。
- **失败非静默 WARNING**：PASS。漏写 LLM 分兜底 `_BACKFILL_DEFAULT_QUALITY`（0.5）时 `logger.warning`（caplog 实测捕获）；clone 失败 / 全失败 / 入口 b 失败均 WARNING + node_errors(degraded)。
- **GlobalState 2 通道为显式 channel**：PASS。`_planning_pending_repo_url` / `_planning_switch_failed` 在 `GlobalState`（TypedDict）显式声明（state.py:204-205）+ `create_initial_state` 显式默认（None/False，state.py:287-288）。GlobalState 为纯 TypedDict（键即通道，无 Annotated reducer 静默丢弃问题）；全量回归 0 KeyError 实证写入不被丢弃。

## 补强用例清单（19 条，tests/test_sprint2_s2_13_boundary.py）
- B1（5+2）：`_normalize_repo_url` 多重尾斜杠 / 协议大小写 / `.git`+尾斜杠组合归一；http vs https 保守不归一；不同 repo 不误判
- B2（2）：多仓库混合成功+失败仅成功入候选；两成功都入、默认选中最后一条（Q-S2-07）
- B3（1）：漏写 LLM 分兜底 `_BACKFILL_DEFAULT_QUALITY` + 非静默 WARNING（caplog 断言）
- B4（4）：switch_failed 生命周期——成功后清 / 入口 a 失败不置 / 入口 b 失败置且不写 0.0 / re-submit 清旧标记
- B5（2）：code_strategy from_scratch→use_repo 纠正；无合并时不强改、不回写 resource_info
- B6（1）：switch_repo 命中既有候选确定性选中（不写 pending、不重抓）
- B7（2）：cancel→cancelled_by_user、approve→approved=True（S2-13 未破坏既有路由）

## Playwright UI e2e 结果（tests/test_sprint2_s2_13_e2e.py，2 passed）
- `test_e2e_switch_repo_failed_shows_force_refill_error`：payload.switch_repo_failed=True → 「🔁 切换仓库」expander 展开 + `st.error`「仓库克隆/分析失败」重填提示渲染（AC-S2-25①）
- `test_e2e_ok_variant_shows_real_quality_and_dash_stars`：候选卡片展示真实 quality_score=0.78（非 0）+ stars/forks「—」占位不崩页（AC-S2-21/23）
- 范式：真起 streamlit 子进程跑 mock-controller harness（env 注入 payload 变体，不依赖真实 LLM / git clone）+ chromium，chromium/子进程起不动则 skip 不 fail；REC 落临时目录不污染仓库
- spinner 文案「正在克隆并分析仓库、重新生成计划……」（switch_repo kind 静态切换）经源码核对 + AppTest 路径覆盖（plan_review.py:823-826）；浏览器侧 spinner 为 rerun 瞬态不稳定捕获，未做硬 DOM 断言（见遗留项 L-S2-13-01）

## 稳定性连跑数据
- browser e2e：2 次连跑均 2 passed（11.89s / 11.94s），无 flaky
- mock 单元（dev 27 + 边界 19 = 46）：2 次连跑均 46 passed（1.38s / 1.37s），无 flaky
- 全量非 e2e 回归：538（基线）→ 559 passed / 0 failed / 25 skipped / 28 deselected / 1 warning（LangChain 预存 deprecation，与 S2-13 无关）

## 失败排查
无。所有用例通过，未发现生产 BUG。

## 后续动作 / 遗留非阻断项
- **L-S2-13-01（非阻断）**：browser e2e 未对 switch_repo spinner 文案做硬 DOM 断言（spinner 是 rerun 瞬态，浏览器侧难稳定捕获）；已通过源码核对 + AppTest 渲染路径覆盖。建议后续若需强证可加 `_await_phase`→spinner 文案映射的纯函数级断言。
- **L-S2-13-02（非阻断）**：真实 LLM/网络 e2e（Task D：planning 维度 Prompt Cache 回归对照 §5.3 + switch_repo 真实 git clone 贯通）因凭证缺失 skip 未跑。注意：S2-13 在 `_PLANNING_SYSTEM_PROMPT_BODY` 追加了「用户提供仓库处理 + 同口径打分」静态引导段（含 `REPO_QUALITY_SCORING_SECTION`），架构 §2.13.2 标注为一次性 breaking 后重新冻结——**凭证就绪后须补跑 planning 维度 cache 回归对照（改动前后命中率），归档本目录**，确认 R-PC4 不回退。该段为 module-level 静态字节（无动态变量，pending_url/feedback 走 HumanMessage 通道），探针已确认 `REPO_QUALITY_SCORING_SECTION` 为静态常量同一对象，预期不破缓存。
- 既有遗留 L-A3-01（test_paper_intake.py 游离）/ L-A3-02（pytest.ini markers 注释）与本次无关，不重复处理。
