# 测试执行报告 - b2-deep-test（resource_scout 深化补全 + 复验收 + 真实链路 e2e）

- **日期**：2026-06-02 16:40（本地时区）
- **执行人**：@测试工程师代理
- **Sprint**：sprint2
- **触发原因**：B2（`core/nodes/resource_scout.py`）在首轮独立验收 PASS（commit df6ccbd，34 用例，报告 `2026-06-02_b2-acceptance.md`）基础上，Maria 要求进一步把测试网做扎实——在已有用例之上找边角缺口补强，并在凭证就绪时跑一次真实链路 e2e 复验收。
- **commit**：df6ccbd（本轮新增用例与 e2e 文件在工作区未提交，git 提交由 Maria 决定）

## 执行范围

- 命令：
  - `pytest tests/test_sprint2_b2.py -q`（B2 单测全量，本轮补强后 61 项）
  - `pytest tests/test_sprint2_b2_e2e.py -m e2e -v -s`（B2 真实链路 e2e，7 项）
  - `pytest -q -m "not e2e"`（非 e2e 核心回归）
- 覆盖用例：
  - 单测 `tests/test_sprint2_b2.py`：上一轮已落 CP-B2-1~10 + aux 3 + `test_acc_*` 14 + `test_deep_*` 16；**本轮新增 `test_deep2_*` 10 项**（其中 1 个 parametrize 计数为 1 函数，单测函数 60、用例 61）。
  - e2e `tests/test_sprint2_b2_e2e.py`：工具层真实网络 3 项 + Prompt Cache 真实 LLM 1 项 + 节点级真实链路 3 项 = 7 项。
- 是否包含 e2e：**是**。凭证来源 env 变量 `LLM_API_KEY` + `DEEPXIV_TOKEN`（由 `tests/conftest.py` 自动从项目根 `.env` 加载，本轮就绪）；`PWC_API_TOKEN` 缺失不阻塞（pwc_tools 设计为匿名访问）。

## 结果摘要

- 通过：B2 单测 61/61；B2 e2e 7/7；非 e2e 核心回归 264/264。
- 失败：0
- 跳过：0（e2e 凭证就绪未触发 skipif；非 e2e 运行下 24 个 e2e 为 deselected，非跳过）
- 警告：1（langgraph 库级预存 `LangChainPendingDeprecationWarning`，sp1 即有，与 B2 无关，跨报告持续记录）
- 总耗时：单测 ~0.81s；e2e ~78.7s（含真实 LLM 节点跑 + 真实 git clone + 真实 PwC）；非 e2e 回归 ~2.35s

## 本轮新增用例（10 项 test_deep2_*，覆盖上一轮 deep 仍漏的边角）

| 用例 | 覆盖缺口 |
|------|---------|
| `test_deep2_select_best_tie_deterministic_first` | 并列最高 quality_score 时 `_select_best_repo` 稳定返回靠前者，5 次调用结果一致（确定性，无随机） |
| `test_deep2_map_selected_repo_tie_uses_first_in_repos` | 经 `_map`：两候选并列 0.7、LLM 漏写 selected_repo 时补的 best 为 repos 靠前者 |
| `test_deep2_selected_repo_not_in_repos_is_preserved` | **selected_repo 指向不在 repos 里的仓库**——锚定当前实际语义：LLM 显式 selected_repo 被透传保留（仅缺失/repos 空时才介入）。非 BUG，是设计语义留痕 |
| `test_deep2_quality_non_numeric_clamped_to_default_zero` | quality_score 为非数值 str / None / dict / 缺键时 `_coerce_float` 退回默认 0.0、不抛错（防 LLM 脏数据炸 max()） |
| `test_deep2_select_best_mixed_invalid_quality_no_crash` | repos 混入脏分（钳为 0.0）时 `_map` 的 max()/select 不崩溃，best 落合法高分者，且不误触发 QUALITY_WARN |
| `test_deep2_backfilled_repo_has_all_12_fields` | **回填 RepoInfo 12 字段完备性**：含易漏的 source/is_official/local_path；缺 source 兜底 "unknown"，仍是严格 12 字段 |
| `test_deep2_degraded_nodeerror_counted_once_empty_repos` | repos 空降级路径本节点 degraded NodeError 恰好 1 条、degraded_nodes 不重复、上游 paper_analysis degraded 保真 |
| `test_deep2_error_field_degraded_counted_once` | error 字段降级路径同样 1 条 degraded、不污染上游 transient（不被改写、不丢） |
| `test_deep2_external_resources_values_coerced_to_str` | external_resources dict 非字符串值被 `_coerce_str` 化（int→str、None→""），JSON 渲染安全 |
| `test_deep2_backfill_none_and_empty_messages_safe` | backfill 在 react_messages=None / 空序列时安全返回 False |

### 与上一轮已覆盖的区分（避免重复）
- **未重复** CP-B2-1~10 勾选与 `test_acc_*` 14 项（工具集 6 工具 / 优先级链 prompt / context 过滤 / wrapper 透传 / backfill 失败前缀 / 混合记录 / QUALITY_WARN 边界 / 钳制）。
- **未重复** `test_deep_*` 16 项（死链跳过 / 非 clone ToolMessage 忽略 / 多成功去重 / list-content 分支 / 缺 local_path 跳过 / None vs 0 指标保真 / SCHEMA required 子集 / enum 恒等 / 二次去重 / notes 不覆盖上游 / Prompt Cache 极端 context / 特殊字符 JSON 安全 / force_finish 只失败 / 回填保留已有 selected）。
- 本轮 `test_deep2_*` 专攻上述两轮都没碰的：**并列分确定性、selected∉repos 语义、非数值分钳制对排序的影响、回填 12 字段完备、degraded NodeError 计数不翻倍、external 值类型归一、backfill 空输入**。

## 真实链路 e2e（凭证就绪，本轮实跑，7/7 PASS）

| 用例 | 验证内容 | 结果 |
|------|---------|------|
| `test_e2e_check_url_reachable_real_dead_vs_alive` | 真实 HEAD：活仓库 True / 死仓库 False，不抛 | PASS |
| `test_e2e_git_clone_and_analyze_small_public_repo` | 真实 git clone octocat/Hello-World + 本地分析，ToolMessage 合法 JSON RepoInfo，local_path 真实落地 | PASS |
| `test_e2e_search_pwc_anonymous_returns_json_contract` | 真实 search_pwc 匿名查询返回合法 `{"results":[...]}` 契约（命中/降级均不破坏） | PASS |
| `test_e2e_prompt_cache_system_body_byte_identical` | **真实跑两篇论文**（HippoRAG / Attention），劫持 ChatOpenAI.invoke 截获真实 SystemMessage，断言每条与冻结主体 `_RESOURCE_SCOUT_SYSTEM_PROMPT_BODY` 字节级一致 | PASS（Prompt Cache 前缀真实不破裂） |
| `test_e2e_resource_scout_real_contract` | 真实 resource_scout 完整跑 arXiv:2405.14831：strategy ∈ 三策略、repos/external 为 list、selected_repo 与 repos 一致性、current_step 正确 | PASS |
| `test_e2e_resource_scout_no_fatal_exception` | 真实链路全程不抛致命异常，本节点 node_error（若有）必为 degraded 非 permanent | PASS |
| `test_e2e_resource_scout_degraded_consistency` | repos 空 ⇔ from_scratch ⇔ 进 degraded_nodes 三者一致 | PASS |

真实链路结论：**搜索优先级链端到端可用**（真实 reachable 探测 + 真实 clone + 真实 PwC 匿名接入 + 真实 LLM 节点编排），节点输出契约成立，降级语义自洽，Prompt Cache 前缀在真实 LLM 调用下字节级稳定。e2e 总耗时 78.7s，单篇真实 LLM 节点跑未爆 token（靶论文/靶仓库均为省 token 设计）。

## 失败排查

无。本轮单测 61/61、e2e 7/7、非 e2e 回归 264/264 全绿，零生产代码 BUG，零测试代码 BUG。

## 验收结论

**PASS（通过）。** 在首轮 34 用例 + 已有 16 个 deep 用例基础上，本轮补 10 个 `test_deep2_*` 填平剩余边角（并列分确定性 / selected∉repos 语义 / 非数值分钳制 / 回填 12 字段 / degraded 计数 / external 值归一 / backfill 空输入），B2 单测达 61 项全绿；**凭证就绪本轮实跑真实链路 e2e 7/7 全 PASS**，端到端验证搜索优先级链可用 + Prompt Cache 真实前缀稳定 + 降级不抛致命异常。零生产 BUG，无需 Maria 决策的阻断项。

## 后续动作 / 遗留（均非阻断）

- 语义留痕（非 BUG，无需 Maria 决策）：`selected_repo` 当 LLM 显式给出不在 repos 内的仓库时，当前实现**透传保留**该值（`test_deep2_selected_repo_not_in_repos_is_preserved` 锚定）。若未来产品要求强制 `selected ∈ repos`，需提需求项并同步改该用例断言——目前 PRD/架构未要求强一致，判定合理。
- 文档轻微不一致（上一轮已记，仍非阻断）：架构 §2.3.2 prompt 示例工具集只列 5 个（漏 search_pwc），权威 §4.6/§2.3.5 + dev-plan 要求 6 个，实现按 6 个落地。建议后续顺手补 §2.3.2 示例。
- B2 本轮新增单测（`test_deep2_*`）与 e2e 文件（`tests/test_sprint2_b2_e2e.py`）仍在工作区未 commit，等 Maria 决定提交。
- 复现 L-A3-02：pytest.ini markers 注释 `--run-e2e` 与凭证驱动实现不符（sp1 既有，与 B2 无关）。
- 下一次跑测试触发条件：B2 进 C1 graph 集成时回归本套；或凭证轮换后重跑 e2e 验证真实链路仍可用。
