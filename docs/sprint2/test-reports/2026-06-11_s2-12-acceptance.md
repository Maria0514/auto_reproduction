# 测试执行报告 - s2-12-acceptance（计划审核页多轮对话敲定修改方向）

- **日期**：2026-06-11 09:10（本地时区）
- **执行人**：@测试工程师代理
- **Sprint**：sprint2
- **触发原因**：S2-12 独立验收（Test Plan Driven，逐条复核 AC-S2-14~19 + 架构裁定落地核对 + 补强边界 + browser e2e）
- **commit**：bffa3d3（被测 `ui/pages/plan_review.py` 落在 2fd3f0c，已在当前 HEAD 历史中）

## 总判定

**PASS**（自动化可覆盖项全部通过；AC-S2-19 为 manual-only，沙箱无 LLM 凭证 + 网络受限，标人工 checklist，未伪造）。

- 架构裁定（方案 A）落地核对：全部 PASS（无 `0.6` / 无 `soft_chat_limit` 残留；软提示判据 `chat_calls >= PLANNING_SOFT_HINT_THRESHOLD(5)`；对话计数不回写 `retry_budget_remaining`、无 `update_state` 写路径）。
- 自动化测试：511 passed / 0 failed（基线 499 + 补强净增 12），25 skipped，28 deselected(e2e)。零退化。
- browser e2e：6 passed（两次连跑均通过，无 flaky）。
- 发现 BUG：无。

## 执行范围

- 命令：
  - 定向：`.venv/bin/python -m pytest tests/test_plan_review_logic.py tests/test_plan_review_e2e.py -q -m "not e2e" -p no:cacheprovider`
  - 全量基线：`.venv/bin/python -m pytest -q -m "not e2e" -p no:cacheprovider`
  - browser e2e：`.venv/bin/python -m pytest tests/test_plan_review_e2e.py -q -m browser -p no:cacheprovider`
- 覆盖用例：
  - `tests/test_plan_review_logic.py`（41 项 = 29 既有 + 12 补强）
  - `tests/test_plan_review_e2e.py`（6 项 browser）
- 是否包含 e2e（真实 LLM/deepxiv）：否。browser e2e 用 mock controller（`RecCtrl`）落盘断言，不调真实模型。AC-S2-19 真实 LLM 多轮对话为 manual-only，未跑（沙箱无凭证 + 网络受限）。

## 逐条 AC 复核结论

| AC | 结论 | 独立实证方式 |
|----|------|-------------|
| AC-S2-14 对话面板替换一次性 textarea | **PASS** | AST 审计 `ui/pages/plan_review.py`：`_review_revise_feedback` / `btn_revise` 已彻底删除（`in src == False`）；改为 `st.chat_input`×1 + `st.chat_message`×1 + 原生 `btn_apply_chat_revision`。四决策按钮文案/key 逐字在位（`✅ 批准计划/btn_approve`、`📄 仅复现代码/btn_code_only`、`🔁 切换仓库/btn_switch_repo`、`⛔ 终止任务/btn_cancel`）。`test_feedback_widgets_are_native_and_appvisible` 验证 chat_input 可见 + switch 键名快照 + 旧键删除。 |
| AC-S2-15 多轮对话实时可见 | **PASS** | `test_chat_turns_never_touch_controller_until_apply`（补强）：mock LLM 连发 3 轮，消息列表正确追加 6 条（user/assistant 交替），chat_calls=3。`test_handle_chat_turn_success_appends` 验证单轮追加。同步 `.invoke` 不起后台线程，主线程不被阻塞（页面其余区块在同一 run 内仍渲染）。 |
| AC-S2-16 敲定方向触发恰一次 revise + 复用现有路径 | **PASS** | `test_apply_chat_revision_resumes_with_summary`：`resume_with` 恰好一次，payload=`{"decision":"revise","user_feedback":<模型纪要>}`，进入 `_KEY_AWAITING=True` / `_KEY_AWAIT_KIND="revise"`。补强 `test_apply_chat_revision_increments_chat_calls_on_success`：总结调用使 chat_calls +1（落定前），落定后清零；复用 `_begin_awaiting("revise", payload)` → `_await_phase` 靠 revise_count 前进判定回审核页（`test_await_phase_revise_waits_until_revise_count_advances`）。planning 决策路由未改（plan_review 不碰 core/）。 |
| AC-S2-17 对话不直接落计划 | **PASS** | `test_chat_turns_never_touch_controller_until_apply`（补强）负向断言：多轮对话期间 `controller.resume_with` / `cancel_task` 零次调用、`_KEY_AWAITING` 不变（仍 False）。plan_review 全文无 `update_state` / 无 `resume_with` 出现在 `_handle_chat_turn` 内（仅 `_apply_chat_revision` / 兜底框 / 四决策按钮才调）→ 对话阶段 reproduction_plan 与 revise_count 不可能被改写。 |
| AC-S2-18 模型不可用降级不崩页 | **PASS** | `test_handle_chat_turn_failure_no_pollution`：invoke 抛错 → st.error 展示降级文案（含「下一步」指引）、保留 user 输入、不追加坏 assistant、计数不变、不崩。补强 `test_fallback_input_triggers_revise_via_apptest`：兜底输入框填一句方向 + 点原生按钮 → `resume_with(revise, user_feedback=兜底原文)` 一次（不经模型总结）；`test_fallback_input_empty_warns_no_revise`：空兜底 → 不 resume + warning。四决策按钮在对话失败时仍渲染可用（同一 run 渲染，无异常）。 |
| AC-S2-19 真实 LLM UI 手动贯通 | **manual-only / 未跑（不伪造）** | 沙箱无 `LLM_API_KEY` / 网络受限，真实 planning 模型多轮对话不可达。标人工 checklist（见下「manual-only 项」）。 |

## 架构裁定（方案 A）落地核对

| 裁定点 | 结论 | 实证 |
|--------|------|------|
| 无 `0.6` 比例阈值残留 | **PASS** | `grep "0\.6" ui/pages/plan_review.py` → 无匹配。 |
| 无 `soft_chat_limit` 残留 | **PASS** | `grep -rn "soft_chat_limit" ui/ core/` → 无匹配。 |
| 软提示判据 = `chat_calls >= PLANNING_SOFT_HINT_THRESHOLD(5)` | **PASS** | 代码 `_render_transparency` L482：`if chat_calls >= threshold`，`threshold = payload.get("soft_hint_threshold") or 5`；payload 的 `soft_hint_threshold` 由 `core/nodes/planning.py:541` 注入 `PLANNING_SOFT_HINT_THRESHOLD`（=`config.py:75` 的 5）。AppTest 实证：chat_calls=5 触发、=4 不触发（`test_chat_calls_soft_hint_at_threshold` / `_absent_below_threshold`）。不使用 `max_total_llm_calls` 比例阈值（仅作 info-bar「LLM 调用上限 N 次」展示）。 |
| 对话计数不回写 `retry_budget_remaining` | **PASS** | `grep retry_budget_remaining ui/pages/plan_review.py` 仅出现在注释（说明「不回写」），无赋值/写路径。`grep update_state` → 无匹配（无主线程 graph 写路径，符合 spike S-2「主线程只读 + worker 独写」）。`_KEY_CHAT_CALLS` 为 UI session 计数器，仅展示 + 软提示。 |

## 补强用例清单（净增 12，落在 `tests/test_plan_review_logic.py` 末尾）

落地前均「先 python 探针验证实现真行为，再写断言」（与项目既有范式一致）：

1. `test_history_to_messages_robust_against_malformed_items` — 非 dict 项跳过 / 缺 role / 未知 role → HumanMessage / content=None → 空串。
2. `test_history_to_messages_none_and_empty` — None / 空列表 → 空序列。
3. `test_handle_chat_turn_non_str_content_coerced` — 助手 content 非字符串（int）→ `str()` 兜底。
4. `test_chat_turns_never_touch_controller_until_apply` — AC-S2-17 强化：对话期间 controller 零写 + awaiting 不变（核心负向断言）。
5. `test_apply_chat_revision_blank_summary_falls_back_to_concat` — 总结返回**空白串（非异常）** → 退化拼接用户发言仍 resume 一次（现有只测了异常路径，此为缺口补全）。
6. `test_apply_chat_revision_no_content_does_not_resume` — 完全空对话 + 总结失败 → 不 resume（不空跑）+ st.error。
7. `test_fallback_input_triggers_revise_via_apptest` — AC-S2-18 兜底框走 revise 的逻辑层断言（AppTest 原生 widget，不依赖浏览器）。
8. `test_fallback_input_empty_warns_no_revise` — 空兜底不 resume + warning。
9. `test_chat_calls_soft_hint_at_threshold` — chat_calls=5 出对话软提示、revise 软提示独立不触发。
10. `test_chat_calls_soft_hint_absent_below_threshold` — chat_calls=4 不出软提示（防 off-by-one）。
11. `test_info_bar_shows_both_revise_and_chat_counts` — info-bar 同列两口径计数 + 上限。
12. `test_apply_chat_revision_increments_chat_calls_on_success` — 总结调用计入 chat_calls（+1 发生在落定清零之前）。

## 结果摘要

- 通过：511（全量 not e2e）+ 6（browser e2e）
- 失败：0
- 跳过：25（全量 not e2e 中既有 skip，非本任务引入）+ e2e 28 deselected（真实凭证 e2e，本环境无凭证）
- 警告：1（`LangChainPendingDeprecationWarning`：langgraph `allowed_objects` 默认值未来变更——项目级既存第三方库 warning，非本任务引入，记录在案，不阻塞）
- 总耗时：全量 70.66s；browser e2e 41.76s（首次）/ 41.81s（复跑）；logic 定向 1.43s

## 稳定性

- logic 文件连跑 3 次：均 41 passed（1.43s / 1.42s / 1.47s），无 flaky。
- 3 个新补强用例单独运行（无运行顺序依赖）：均 1 passed。
- browser e2e 连跑 2 次：均 6 passed，无 flaky。

## 失败排查

无失败。

## browser e2e 覆盖核对

`tests/test_plan_review_e2e.py`（6 项，真起 streamlit 子进程 + chromium，本环境 chromium 可启动，真跑通）：
- `test_e2e_approve` / `test_e2e_code_only` / `test_e2e_cancel_first_click_no_cancel_task` / `test_e2e_cancel_confirm_calls_cancel_task` — 四决策按钮路由正确。
- `test_e2e_revise_via_fallback_input` — S2-12 后改走对话面板内**兜底输入框**断言 revise（对话轮次需真实模型、harness 无凭证会降级，故走确定可断言的兜底路径，同时覆盖 AC-S2-18 兜底能力）。按 label「一句话修改方向（兜底）」精确定位，避开 chat_input。
- `test_e2e_switch_repo_carries_feedback_and_url` — switch 反馈/URL 按 label 精确定位（避开 S2-12 后主文档新增的 chat_input textarea 与兜底输入框）。

开发对 e2e 的两处改动（revise 改走兜底 + switch label 精确定位）均真实覆盖且本环境可跑，定位策略正确（不再用「第一个 textarea/input」粗暴命中）。

## manual-only 项 checklist（AC-S2-19，需真实 LLM 凭证 + 网络）

> 沙箱不可执行，移交 Maria 在有凭证环境手动验证：
- [ ] `streamlit run app.py`，走到计划审核页（planning interrupt 态）。
- [ ] 在「✏️ 与规划助手讨论修改方向」面板与真实 planning 模型多轮对话（≥2 轮），逐条回复实时可见、可继续输入。
- [ ] info-bar「本轮对话已消耗 X 次调用」随对话递增；连发 ≥5 轮出现对话软提示 warning。
- [ ] 点「✅ 确定方案并重新生成计划」→ 进入「正在根据你的修改意见重新生成计划……」轮询态 → 重规划完成后回审核页展示**新计划**（revise_count 前进 1）。
- [ ] 对话失败场景（断网/错配 key）→ 降级文案 + 「下一步」指引 + 兜底输入框仍可提交 revise。

## 后续动作

- 无阻塞项。511 passed 基线 + 补强 12 可随报告一并由 Maria 提交。
- 遗留观察：项目级 `LangChainPendingDeprecationWarning`（langgraph `allowed_objects`）长期存在，建议后续统一在升级 langgraph 时处理（非本任务范围）。
- 测试副作用产物：运行 e2e/AppTest 后工作区出现 `checkpoints.db-shm` / `checkpoints.db-wal`（WAL 模式临时文件），非源码变更，提交时忽略。
- 下次触发条件：plan_review.py 或 core/nodes/planning.py revise 路径变更后回归 `tests/test_plan_review*.py`。
