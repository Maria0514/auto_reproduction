# 测试执行报告 - b1-deep-test

- **日期**：2026-06-02 02:10（本地时区）
- **执行人**：@测试工程师代理
- **Sprint**：sprint2
- **触发原因**：Maria 要求在 B1 首轮验收 PASS（16 用例，报告 `2026-06-02_b1-acceptance.md`，commit baef535）基础上做**第二轮深化测试完善 + 复验收**，目标把 B1 测试网做扎实，不重复已勾选的 CP-B1-1~10
- **commit**：baef535（B1 已提交；本轮新增用例与 B2 工作区改动均未提交）

## 执行范围

- 命令：
  - `pytest tests/test_sprint2_b1.py -v`（B1 单文件，含新增深化用例）
  - `pytest -q -m "not e2e"`（核心回归，连跑 3 次）
  - `pytest -m e2e -rs`（确认 e2e 在凭证缺失下的跳过/离线行为）
  - 单用例 / 单 parametrize / 单文件连跑（独立性 + 抖动核查）
  - 落地前用独立 python 探针脚本预验证 5 类边界的实际行为（避免基于错误假设写断言）
- 覆盖用例：`tests/test_sprint2_b1.py`（16 → 30 项）；新增 8 个测试函数（其中 2 个 parametrize 各 3 变体 → 采集 14 个新测试项）
- 是否包含 e2e：**否（真链路命中率回归本轮无法跑，凭证缺失，详见"命中率回归"段）**

## 本轮在 16 用例之上新增的深化覆盖（缺口 → 用例）

| 缺口（上一轮未覆盖） | 新增用例 | 关键证据 |
|---|---|---|
| *_zh 三态：缺键 / 显式 None / 空串（上一轮只测缺键） | `test_deep_b1_intake_zh_tristate_all_trigger_backfill`（parametrize×3） | 三态都触发回退，恰好 1 条 degraded NodeError；锁死 `not get()` 语义，防止改成 `field not in meta` 漏掉空串/null |
| *_en 三态同上（analysis 侧） | `test_deep_b1_analysis_en_tristate_all_trigger_backfill`（parametrize×3） | 三态都回退到中文主字段并标 degraded |
| 主字段本身缺失时 *_zh 不应回退出空串（dev-plan 深化重点点名） | `test_deep_b1_intake_no_backfill_when_main_field_absent` | tldr 主字段为 None 时 tldr_zh 保持 None（不被填 ""）；title/abstract 仍正常回退 → 逐字段判定 |
| 主字段缺失（analysis 侧 hardware_requirements 空） | `test_deep_b1_analysis_no_backfill_when_main_field_absent` | hardware_requirements 空 → hardware_requirements_en 不回退假值；method_summary 仍回退 |
| 多主字段同缺的精确逐字段语义 | `test_deep_b1_intake_no_zh_backfill_and_no_degraded_when_all_main_absent` | title 非空回退、abstract/tldr 主字段空时对应 *_zh 不回退；至少一项回退仍 degraded |
| **react_messages 工具回填与 *_en backfill 的执行顺序交互**（dev-plan 深化重点） | `test_deep_b1_analysis_tool_backfill_then_en_backfill_order` | method_summary 漏写 + 工具历史有 read_section → 工具回填补全 method_summary 后，method_summary_en 回退到**工具回填后的值**而非空；且因工具回填已补全核心字段，**不再产生 missing core degraded**（证明回填在判降级之前）。若有人把 *_en backfill 提到工具回填之前，本用例立刻失败 |
| degraded 多源叠加去重 + NodeError 计数（analysis 侧无工具历史时） | `test_deep_b1_analysis_triple_degraded_dedup_and_count` | *_en 回退(1 条) + missing core(1 条) = 2 条语义不同 degraded NodeError，degraded_nodes 去重为单次 |
| Prompt Cache 前缀在极端 context（None / 空 dict / 特殊字符 / 非 dict）下字节稳定 | `test_deep_b1_prompt_prefix_stable_under_extreme_contexts` | 5 类极端 context（含 `{}`、引号、换行、中文、paper_meta=非 dict）截尾后前缀全部 == `BODY + "\n" + SECTION`，互相字节级一致 |
| intake prompt 任意 context 完全字节级一致 | `test_deep_b1_intake_prompt_fully_static_under_any_context` | 5 类 context（空 dict / 特殊字符 user_input / 无关 key / 缺键）下输出唯一字节串 |
| 空串形态回退也打 WARNING（非静默不被空串绕过） | `test_deep_b1_intake_empty_string_zh_still_warns` | caplog 断言空串 *_zh 回退仍打 WARNING |

**落地前探针实证（python 直跑，确认实现真行为再写断言）**：

- *_zh 三态（empty/none/missing）：三态均回退主字段值，各打一次 WARNING —— 实现健壮。
- tldr 主字段缺失：`tldr=None` 时 `tldr_zh=None`（**未被填成空串**）—— dev-plan 担心的"回退出空串"在实现中不存在。
- 顺序交互：method_summary 漏写 + 工具历史 read_section → method_summary 被工具内容补全，method_summary_en 随后回退到该补全值（非空）—— **执行顺序正确（工具回填先于 *_en backfill 先于 missing 判定）**。

## 结果摘要

- 通过：B1 单文件 **30**（16 原有 + 14 新增项）；核心非 e2e 全量 **221**（连跑 3 次：221 / 221 / 221）
- 失败：0
- 跳过：0（17 个 e2e 为 deselected，非 skip）
- 警告：1（langgraph 库级预存 `LangChainPendingDeprecationWarning`，sp1 即有，与 B1 无关）
- 总耗时：B1 单文件约 0.88s；核心回归约 2.4s/次

## 命中率回归（Prompt Cache 真链路，本属 E2 阶段，本轮尝试提前跑）

- **凭证检查**：`config.get_llm_api_key()` → **EMPTY**；`config.get_deepxiv_token()` → **EMPTY**（.env 与 ~/.env 均无有效值）。
- **结论：本轮无法跑真链路命中率回归，明确跳过，不伪造任何数据。** S-3 基线 R=0.7669 / 守门 AC-S2-08 ≥0.7286 的对照实验需在凭证就绪后于 e2e 验收补跑。
- **已提供的前置安全垫（无需凭证即可证）**：本轮在原 CP-B1-5 字节级一致基础上，新增 `test_deep_b1_prompt_prefix_stable_under_extreme_contexts` —— 在 None / 空 dict / 特殊字符 / 非 dict 等极端 context 下证明 system prompt 截尾后前缀 == `BODY + "\n" + _LANGUAGE_POLICY_SECTION` 字节级一致。前缀字节稳定是命中率不退化的**充分前置条件**（自动型 prompt cache 以前缀字节为匹配单位）。命中率最终数值仍需真链路验证，但前缀漂移风险已被本轮进一步排除。

## 失败排查

无失败。

## 工作区状态观察（非 B1 范围，记录在案）

- 工作区检出别的 agent 未提交改动：`core/nodes/resource_scout.py`（B2 新增）、`tests/test_sprint2_b2.py`（18 项）、`core/nodes/__init__.py`、`docs/TODO.md`。本轮核心回归总数 221 = sp1+sp2 已提交基线（含 B1-16）+ B2 工作区 18 + 本轮 B1 新增 14。
- **本轮自身 diff 严格隔离**：`git diff --stat` 确认仅触及 `tests/test_sprint2_b1.py`（+323 行 / 1 file），未触碰任何 `core/` 生产代码与 B2 产物。
- B2 文件 18 项随回归一并跑通（顺带回归确认，不在本轮验收范围）。

## 遗留与后续动作（沿用上一轮，无新增阻断）

- **E2 命中率回归仍待跑**：凭证就绪后跑 paper_analysis 真链路，对照 S-3 R=0.7669 / 守门 ≥0.7286，验证语言策略段落落地后命中率不退化。本轮已把前缀字节稳定证据从"2 篇正常论文"加强到"5 类极端 context"。
- **e2e 治理观察（非 B1，非阻断）**：`pytest -m e2e` 在凭证缺失下，正确 skip 的用例与少量纯离线结构用例（graph 拓扑 3 个 + paper_analysis paper_meta=None 短路 1 个）混跑；但发现 e2e 全量跑会在某 fixture 处耗时较长（>90s 被 timeout 截断），疑似 graph fixture 初始化耗时。属 e2e 套件治理范畴，与 B1 无关，建议 E 阶段排查。
- **pytest.ini 文档与实现不符（沿用 L-A3-02，本轮复现）**：markers 注释写"加 `--run-e2e` 启用"，但实现是凭证 skipif 驱动 + `-m e2e` 选择。建议修订注释，避免误导。
- B1 改动已 commit（baef535）；本轮新增用例与 B2 产物待 Maria 触发提交。

## 验收结论

**PASS（深化轮）**。在首轮 16 用例基础上新增 8 个测试函数（采集 14 项），系统性覆盖了上一轮验收报告与 dev-plan 深化重点点名的全部缺口：*_zh/*_en 三态语义、主字段缺失时不回退空串、工具回填与 *_en backfill 的执行顺序交互（最关键）、多源 degraded 叠加去重的 NodeError 计数、Prompt Cache 极端 context 前缀字节稳定。落地前以 python 探针预验证实现真实行为，所有断言均与实现一致。核心回归连跑 3 次 221/221 零失败零跳过零抖动，B1 单文件 30/30 独立可跑无序依赖。**零 BUG** —— 实现对所有边角场景（三态、主字段缺失、执行顺序）均表现健壮。命中率真链路回归因凭证缺失明确跳过（未伪造），前缀字节稳定证据已加强。无需 Maria 决策的阻断点。
