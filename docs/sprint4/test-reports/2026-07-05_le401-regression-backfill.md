# L-E4-01 闭环回归补记（backfill）

- **日期**：2026-07-05（补记；实际回归执行于 2026-07-04，随 commit `86c5325` 落地）
- **性质**：归档补记——本轮回归当时仅记录于 `docs/TODO.md` 与 commit 信息，未按测试报告归档规范落盘，本文件为对账补档，不含新执行的测试
- **关联**：挂账项 L-E4-01（架构师裁决落地）| commit `86c5325` "fix(s4): L-E4-01 闭环——_effective_runs 同命令取最后一次"

## 变更内容（86c5325）

- **裁决**：方案甲收敛版——同命令（argv 精确匹配）重试以最后一次执行为准，覆盖跨 interrupt 与子图内重试两形态；命令变体两条都保留 → failure（fail-safe）
- **落点**：`core/nodes/execution.py` 新增 `_effective_runs` helper + 三处一行接入（不改签名）；logs/runtime 仍全量保留，失败证据不丢；B 档判定只认编排层红线、不回退
- **文档**：`docs/sprint4/architecture.md` §3.2 补口径注记

## 测试与回归结果

| 项 | 结果 |
|---|---|
| 新增测试 | `tests/test_sprint4_le401_fix.py`（158 行；边界 8 条，含参数化） |
| 既有 characterization | 保留为边界锚定；主叙事剧本翻转（重试成功 → success、无二次 interrupt#2），`tests/test_sprint4_e4_regression_gate.py` 同步改造 |
| 全量回归 | **1349 passed / 37 skipped / 0 failed**（基线 1341 + 8） |
| 幂等专项 | 幂等类 122 条 × 3 轮连跑全绿 |

## 数字链对账

e3-e4 验收报告（2026-07-04）止于 **1341** → 本轮 L-E4-01 闭环 **+8** → **1349**。
自此测试数字链 1119 → 1140 → 1197 → 1224 → 1229 → 1238 → 1311 → 1341 → **1349** 在报告层全程有档可查，G1 守门以 1349 为回归基线。

---
*补记人：主控代理（2026-07-05 文档一致性审查修复的一部分；数据来源：commit 86c5325 提交信息 + docs/TODO.md Sprint 4 节记录）*
