# Sprint 3 任务 F2 / CP-F2-2 真实链路 e2e 补跑报告

- **日期**：2026-06-29
- **执行**：@主控（凭证就绪后补跑；沿用 sp2 范式——主控补跑、据实归档）
- **靶论文**：arXiv:2405.14831（HippoRAG）
- **测试**：`tests/test_sprint3_e2e.py::TestRealChainE2E`（real-1~5，参数化共 7 个 test item）

---

## 裁决：CP-F2-2 PASS —— 7/7 真实 e2e 全绿

### 真实 / mock 边界（dev-plan §667 权威）
- **真实**：LLM（intake/analysis/scout/planning/coding 各 ReAct + metrics 抽取）、deepxiv（read_section / get_paper_structure 读 HippoRAG）、`build_graph()` 主图、SqliteSaver(WAL)、execution 复合节点、reporting 三形态渲染。
- **mock**：仅 sandbox 三入口（prepare_venv / run_in_venv / collect_artifacts）—— happy path B 档 = mock sandbox 返回 exit 0 + 可解析 `<METRICS>`，**不真跑 30min 训练**。
- 故本报告验证的是「**真实 LLM 链路 + 主图编排 + 各节点真实产出能端到端打通**」，**非**真跑出论文实验数据。

### 跑数据
| 轮次 | 范围 | 结果 | 耗时 |
|---|---|---|---|
| 首轮 | 全套 7 item | 6 passed / 1 failed | 29m05s |
| 修复 | real-1 断言 `run==1` → `run>=1` | 测试断言过严（**非生产 BUG**）| — |
| 重跑 | real-1 单条 | **PASSED** | 3m08s |
| **合计** | **7/7 真实 e2e 通过** | | |

**real-1 首轮失败根因**：断言 `cnt["run"]==1` 假设 execution 只跑 1 条命令，但真实 LLM 规划出 **11 步** `execution_steps`，execution 逐步 `run_in_venv` → run 调 11 次。失败行**之前**的核心契约断言（`success=True` / `metrics.accuracy=0.893` / 跑到 END / coding 真实产出 code_output_dir）均已通过——系统行为正确，纯测试断言过严。修复为 `run>=1`（不 hard-code 步数）后重跑全绿（含 reporting full_success 断言）。

### 各场景结果
| 用例 | 场景 (AC) | 结果 |
|---|---|---|
| real-1 | happy path B 档成功 (AC-S3-01) | ✅ PASSED（断言修复后重跑确认）|
| real-2 | 修复循环上限 3 (AC-S3-03) | ✅ PASSED |
| real-3 | interrupt#2 三态 terminate/revise_plan/export_code (AC-S3-07) | ✅✅✅ PASSED |
| real-4 | code_only 跳过 execution (AC-S3-06) | ✅ PASSED |
| real-5 | 降级 (AC-S3-09③) | ✅ PASSED |

### 凭证 / 配额
- 凭证有效：真跑通，无 401/429。
- deepxiv 配额够：首轮 29min + 重跑 3min 全程**无 `DailyLimitError`**；HippoRAG 缓存命中（只读已缓存章节）。

### LLM 服从度观察（非阻塞，记未来优化项）
日志显示 LLM 多次用**错章节名**读 deepxiv（猜 `Method`/`Experiments`/`4`/`3 Method`/`5 Experiments`，而 HippoRAG 实际章节为 `HippoRAG`/`Experimental Setup`/`Results` 等），触发 deepxiv `section not found` 降级。agent 有兜底（get_full_paper 等）继续，复现未受阻。说明 `read_section` 章节名命中率有优化空间。另有 pwc API 200 非 JSON 警告（pwc_tools 已容错）。

### §674 稳定性复跑
real-2 / real-3（interrupt#2 + 修复循环，属 LLM 服从度类）本次**各跑 1 次通过**。§674 要求的连跑 3 次稳定性加固，经 Maria 决策**省 deepxiv 配额、记为可选待补**（非阻塞——单次已验证逻辑正确）。

### 已知 warning
- langgraph 库级 `LangChainPendingDeprecationWarning`（sp1 即存，非新引入）；
- checkpoint 反序列化 `ExecutionMode`/`SecretStr`/`ChatOpenAI` 未注册类型告警（langgraph 未来版本兼容性，已知，非阻塞）。

### 相关
- 代码落地报告：`test-reports/2026-06-29_f2-real-e2e-wiring.md`
- mock e2e（CP-F2-1）：`tests/test_sprint3_e2e.py` 8 条不标 e2e，进常规回归。
